target/
Cargo.lock
# src/model.rs is hand-curated (multi-fixture renames cannot be
# auto-generated by aeneas-check --rust-model in one shot — see
# scripts/regen-diff-models.sh).
# Per-fixture auto-generated model files (Phase 1C: ADT fixtures
# get their own model file because their crate-local struct decls
# can't share a flat namespace).
src/aggregates_basic_model.rs
src/reborrows_model.rs
