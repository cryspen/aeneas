## Plan: M10 LLBC# Soundness
## Started: 2026-05-17
## Last action: Session 7 wrap. Tip 49ef3650. 2 commits: M10.2r (bundle C18-C22 per-entry JoinEntryStep helpers in a new `JoinLemmas.lean`) + M10.2s (C23 `stepJoin_witnessed_sound`, empty-witnesses subset). Phase-C count advances 17/23 → 23/23 lemmas declared, with 5 trivial (C18-C22) + 1 partial (C23 empty-witnesses subset only — the general case with non-empty state-changing witness rules is the campaign's deepest open issue, surfaced below). Only G6 sorry left is the top-level `stepEvent_sound` (D).
## Phase: C (23/23 lemmas declared; C23 partial)
## Next commit: Decision point. **(Option A)** M10.2t Soundness: stepJoin general-case attempt — this is the M9.8 schema-bump candidate. Likely needs explicit user sign-off (see Open design decision below) before a Lean attempt makes sense. **(Option B)** M10.3a Phase-D `stepEvent_sound` — mechanical case-split delegating to the existing per-event lemmas. Most per-event lemmas now take Phase-D-dischargeable hypotheses (`hShape`, `hFresh`, `hStShape` style), so Phase D's real work is *discharging* those hypotheses from `CertGen_faithful`. This is the natural next step if escalating the C23 general case to the user; Phase D can proceed using the empty-witnesses C23.

### Completed commits (Session 1 — 2026-05-17)
- [x] M10.0a Soundness: scaffold aeneas-lean-soundness Lake package + Mathlib pin — c0a0413a — 2026-05-17
- [x] M10.0b Soundness: port LLBCSharpPaper.Syntax (Val#, Place#, RegionAbs) — d0355236 — 2026-05-17
- [x] M10.0c Soundness: port LLBCSharpPaper.State (LLBCState, ctx, abs maps) — 4ad9f22d — 2026-05-17
- [x] M10.0d Soundness: define LLBCSharpPaper.Program helpers (lookupFunDecl etc.) — 867a2cae — 2026-05-17
- [x] M10.0e Soundness: port LLBCSharpPaper.WellFormed (+ WellFormedProgram) — 3e1bbce9 — 2026-05-17
- [x] M10.0f Soundness: port LStep Fig. 3 rules — bd1cf78e — 2026-05-17
- [x] M10.0g Soundness: port LStep Fig. 7+8 rules (reborrow, call, endAbs, symExpandMutBorrow) — 446605bb — 2026-05-17
- [x] M10.0h Soundness: port LStep Fig. 11 join rules — d45180d0 — 2026-05-17
- [x] M10.0i Soundness: port LStep §5.2 loop rule (loopInv) — afbe4624 — 2026-05-17

### Completed commits (Session 3 — 2026-05-17)
- [x] M10.2a Soundness: stepPanic / stepRetn soundness (trivial passthroughs) — cf85dc4d — 2026-05-17
- [x] M10.2b Soundness: stepMatchArm / stepLoopEnd soundness (no-op markers) — a09e34c8 — 2026-05-17
- [x] M10.2c Soundness: stepMove / stepCopy soundness — f7b3ee49 — 2026-05-17
- [x] M10.2d Soundness: stepAssign soundness — e1c834c5 — 2026-05-17
- [x] M10.2e Soundness: stepAssert soundness (true/false branches) — a25d2f06 — 2026-05-17
- [x] M10.2f Soundness: stepBinop soundness + bumpSymValId redefined as no-op — a7d4b2a3 — 2026-05-17

### Completed commits (Session 4 — 2026-05-17)
- [x] M10.1f Soundness: close concretise_addLoan via maxKeyPlusOne_insert_fresh — 9be15ea7 — 2026-05-17 (sorry-filler-deep agent in worktree, auto-merged)
- [x] M10.2g Soundness: stepSharedBorrow soundness (C7) — f645fe49 — 2026-05-17
- [x] M10.2h-j Soundness: stepMutBorrow direct/inAbsReborrow/loopOwned (C8-C10) — 3e5e3318 — 2026-05-17 (batched: three M9.6 hint variants share guards, freshness, and the concretise_addLoan chain)

### Completed commits (Session 5 — 2026-05-17)
- [x] M10.1g Soundness: add loanIdHwm to SymState (monotone HWM for nextLoanId) — 8c6e54ff — 2026-05-17 (checker-side addition; replayer behavior unchanged)
- [x] M10.1h Soundness: retarget concretise.nextLoanId at loanIdHwm; close concretise_takeLoan — 6f2d4f4d — 2026-05-17 (concretise_addLoan now unconditional)
- [x] M10.2k-m Soundness: stepEndBorrow direct/reborrow/shared (C11-C13) — ee95913c — 2026-05-17 (batched: shared takeLoan-commute discharge + Phase-D-dischargeable result-shape contract)
- [x] M10.2n Soundness: stepReborrow soundness (C14) — 4a5415af — 2026-05-17 (case-splits on `loans.contains parent`; both branches fold to `Ω.bumpLoanId child` via `hParentInHwm` no-op argument)
- [x] M10.2o Soundness: stepCall soundness, empty-absSig subset (C15a) — 922c0bb0 — 2026-05-17 (Phase-A surface gap: LStep.call doesn't install absSig in post-state; restricted to absSig = #[] case)

### Completed commits (Session 7 — 2026-05-17)
- [x] M10.2r Soundness: bundle C18-C22 per-entry JoinEntryStep lemmas — 712befaa — 2026-05-17 (six paper-side constructor wrappers in a new `AeneasSoundness/Soundness/JoinLemmas.lean`; deviates from plan §3.2's 5-commit cadence because per-entry lemmas turned out to be one-line constructor applications, not 250-LOC replayer-correspondence proofs — see "Design realisation" note below)
- [x] M10.2s Soundness: stepJoin_witnessed_sound (C23, empty-witnesses subset) — 49ef3650 — 2026-05-17 (`witnesses = #[]` reduces the chain to `JoinChain.nil`; Phase-D-dischargeable `hStShape : concretise st' = concretise st` discharges the wholesale-replace correspondence; general case escalated to user — see Open blocker)

### Completed commits (Session 6 — 2026-05-17)
- [x] M10.1i Soundness: SymState.absIdHwm + stepEndAbs erases registry; move liftAbsShape paper-side — 9a6ca9b7 — 2026-05-17 (paired replayer + concretise infrastructure for the M10.0m revision; commute lemmas `concretise_addAbsShape` and `concretise_removeAbsShape` land here)
- [x] M10.0m+M10.2o-revised Soundness: LStep.call installs absSig; re-prove C15 in the full case — 89e75578 — 2026-05-17 (bundled because the LStep.call shape change forces an immediate C15 re-prove; fold-then-setLocal-then-bumpSym; new `concretise_foldl_addAbsShape` commute via Array→List.foldl induction)
- [x] M10.2p Soundness: stepEndAbs soundness (C16) — f5d4de11 — 2026-05-17 (Phase-D-dischargeable `(stPre, hConcPre, hShape)` triple folds the loan-erase + token-clear preamble; chain `concretise_removeAbsShape` ; `congrArg (·.removeAbs absId) (hConcPre.trans hRep)`)
- [x] M10.2q Soundness: stepSymExpandMutBorrow soundness (C17, no-substitution subset) — debe8ec3 — 2026-05-17 (`substLocals = #[] ∧ substLoans = #[]` Phase-D-dischargeable; one η-fold subtlety on the `{ env := st.env, ... } = st` literal record handled by `have hEtaSt ... := rfl`)

### Completed commits (Session 2 — 2026-05-17)
- [x] M10.0j Soundness: port Valid predicate (per-event premise extractor) — b099d439 — 2026-05-17
- [x] M10.0k Soundness: replace 4 axioms in StepEventSound.lean with real types — c7f8d8fb — 2026-05-17
- [x] M10.0l Soundness: Phase-A boundary review janitorial fixes — 28087ada — 2026-05-17
- [x] M10.1a Soundness: define concretise — env + numLocals — 7f1090af — 2026-05-17
- [x] M10.1b Soundness: define concretise — loans + absRegistry — 843cc93b — 2026-05-17
- [x] M10.1c Soundness: prove concretise_wellFormed_smoke — 7c2119e9 — 2026-05-17
- [x] M10.1d Soundness: concretise inversion lemmas — ac015ca8 — 2026-05-17
- [x] M10.1e Soundness: concretise commute lemmas (setLocal proved; addLoan/takeLoan sorry'd) — c670f691 — 2026-05-17

### Phase A complete (11+1 commits: M10.0a–k + janitorial M10.0l)
All paper-side surface (Syntax, State, Program, WellFormed, Step, Valid)
ported. `StepEventSound.lean` typechecks against real types; 5 sorry'd
theorems remain (4 per-event lemmas + top-level stepEvent_sound),
closed by Phases C/D.

### Phase B complete (M10.1a–e)
`concretise : SymState → LLBCState` fully defined:
- env → ctx via `liftVal` / `liftEnv`
- absRegistry → abs via `liftAbsShape` / `liftAbsRegistry`
- freshness counters from `maxKeyPlusOne` over loans + absRegistry
  (nextSymValId = 0; cert-provided monotonicity rides on CertGen_faithful)

Five inversion lemmas (`@[simp]`-tagged) read concretise's structure
field by field. setLocal commute proves; addLoan / takeLoan commute
sorry'd as Phase-C-driven discharges (hashmap-fold lemma needed).

Smoke lemma `concretise_wellFormed_smoke` discharges via
`empty_concretise + empty_WellFormed`.

### Trusted-base inventory (live)

The four "paper-theorem placeholder" axioms below are introduced by
Phase A (when Phase A's Step.lean grows them — currently *not yet
introduced*; the JoinDeterminisation hazard called out in plan
§11.1 #1 may seed one of them earlier) and replaced (one-by-one) by
Phase G ports if/when Phase G is undertaken. Until then they remain
as the *expected* trusted extension; G5 enforces no others.

| Axiom | Reason | Replaced by | Status |
|---|---|---|---|
| `CertGen_faithful` | OCaml-side honesty (broadened post-M9.7) | (forever trusted) | not yet declared (lands when first per-event lemma needs it) |
| `paper_thm_3_1_confluence` | Phase A placeholder | Phase G M10.6d | not yet declared |
| `paper_thm_3_3_pl_refines`  | Phase A placeholder | Phase G M10.6e | not yet declared |
| `paper_thm_4_1_safe`        | Phase A placeholder | Phase G M10.6f | not yet declared |
| `paper_thm_4_2_init`        | Phase A placeholder | Phase G M10.6g | not yet declared |

Current axiom set per `#print axioms AeneasSoundness.Soundness.stepEvent_sound` (golden:
`aeneas-lean-soundness/tests/axioms.golden.txt`):

```
propext, sorryAx, Quot.sound
```

Post-M10.0k all four domain axioms (LLBCState, LStep, Valid,
concretise) are gone. `stepEvent_sound` itself is now a `theorem`
witnessed by `sorry`; Phase D (M10.3a) closes it.

### Blockers escalated to user

**ACTIVE (Session 7, 2026-05-17) — C23 general case = M9.8 decision needed**:

C23 (`stepJoin_witnessed_sound`) closes in Session 7 for the
`witnesses = #[]` subset. The general case, where witnesses contain
state-changing rules (`joinSymbolic` or `joinMutBorrows`), surfaces
the long-anticipated "fresh abs" gap from plan §11.1 #1 / §3.4 /
§14.1.

The semantic divergence:
* Paper's `LStep.join` chains per-entry `JoinEntryStep` rules. For
  `joinMutBorrows l_left l_right l_fresh abs`, the rule's post-state
  installs `Ω.abs abs := some { roles := {(.mutBorrow, l_left),
  (.mutBorrow, l_right), (.mutLoan, l_fresh)}, parents := #[] }`.
* Replayer's `stepJoin` (Step.lean:506-554) does NOT allocate this
  abs in `absRegistry` — it only wholesale-replaces `env` /
  `loans`. So `concretise st'` has *no* abs entry for `abs`, but
  the chain's terminal Ω' has one.

This means `concretise st' ≠ chain-terminal` for any
`joinMutBorrows` witness, breaking the load-bearing conclusion of
the lemma.

The plan anticipated three resolution paths (§14.1, §3.4, §11.1
#1); the M9.8 decision is:

**(A) — M9.8 schema bump (recommended by plan §14.1)**. Bump
   `fmt_version` to 4. Extend `JoinRule.joinMutBorrows` to carry an
   explicit `absShape : AbsShape` (or just the role list `roles :
   Array AbsRoleEntry`). Then `stepJoin` allocates the abs in
   `absRegistry` using the new payload; concretise matches; the
   per-entry helper `joinMutBorrows_step` in `JoinLemmas.lean`
   stays as-is; C23 general case closes via normal induction. Cost:
   one OCaml emitter change + cert regen for the 89 fixtures + one
   replayer extension. ~1 week of work outside the soundness
   campaign.

**(B) — Broaden `CertGen_faithful` to cover the abs creation**.
   Extend the axiom's promise so that when the cert emits
   `joinMutBorrows ... abs`, the OCaml interpreter has indeed
   created the matching `AbsShape` and the soundness side may
   *assume* it. Then add a Phase-D-dischargeable hypothesis
   `hAbsCreated : st.absRegistry[abs] = some { roles := ..., ... }`
   to the C23 lemma signature and discharge via the broadened
   axiom. **Trust drift** — the axiom now covers more than its
   §0.3 charter. Per plan §6 hard rule, this requires explicit
   user sign-off.

**(C) — Restrict C23 to no-op witness rules only (`joinSame`,
   `joinVar`, `joinBottomOther`, `joinOtherBottom`)**. Add an
   explicit `hAllNoOp : ∀ e ∈ witnesses.toList, e.rule.isNoOp =
   true` hypothesis. Phase D then has to verify this from cert
   data; many real fixtures will fail (every cert that joins on a
   mutBorrow has a `joinMutBorrows` entry). Punt to M11+.

**Recommendation**: ship Phases A–F at the empty-witnesses subset
of C23, treat option (A) as M11 / M9.8 follow-up. The end-to-end
`cert_implies_pl_safety` corollary then quantifies over crates whose
joins are degenerate (empty witnesses), which is a much weaker
guarantee but still ships the verified-pipeline architecture and
the trust skeleton. Then M9.8 happens after M10.5c as a focused
schema-bump campaign, and a follow-up M11.X closes C23 unrestricted.

A Lean attempt at option (A) is contingent on the user picking it
(the cert format is the user's call). Option (B) is a hard blocker
without user sign-off. Option (C) is doable autonomously but
weakens the corollary further.

**Next session's decision**: orchestrator should escalate to the
user *before* attempting any non-trivial subset extension of C23.
A reasonable alternative is to skip ahead to Phase D using the
empty-witnesses C23 (Phase D doesn't depend on C23's generality —
it just dispatches on `Event` constructors and passes whatever
hypotheses each per-event lemma takes; if Phase D's
`stepEvent_sound` ever encounters a non-empty join in practice, it
will fail to apply C23 and the failure is observable).

**RESOLVED (Session 6, 2026-05-17)** — the M10.0m Phase-A revision
landed cleanly. `LStep.call` now installs `absSig` in its post-state,
`stepEndAbs` erases the registry entry on the replayer side (M10.0n
folded into M10.1i), and C15 is re-proved unrestricted. C16
(stepEndAbs) and C17 (stepSymExpandMutBorrow no-substitution subset)
also closed. No active blockers; Session 7 opens cleanly with C18
(`joinSame_sound`) or the C18-C22 sibling-file refactor.

**(Historical) Phase-A inflection point (Session 5 wrap, 2026-05-17)** — surfaced
by C15 (stepCall) and confirmed by inspection of C16 (stepEndAbs):

* `LStep.call` (paper Step.lean:390-395) declares its post-state as
  `(Ω.setLocal dst.local_ (.sym σ)).bumpSymValId σ` — i.e., a single
  dst-write with no abs installation. But the replayer's `stepCall`
  folds the cert's `absSig` into `absRegistry`. The two sides
  diverge as soon as `absSig ≠ #[]`.
* `LStep.endAbs` (paper Step.lean:407-412) takes `Ω.abs abs = some r`
  as premise. Until `LStep.call` actually installs abs, this premise
  is vacuous (`Ω.abs` is identically `none`) and C16 has nothing to
  fire against.

The plan §3.4 explicitly anticipates Phase-C-driven Phase-A
strengthening: "if a per-event lemma needs one, the Phase-C commit
adds the corresponding `WellFormed` field". The strengthening
required here:

1. **Paper-side (LLBCSharpPaper)**: extend `LStep.call`'s post-state
   to fold `setAbs absSig[i].absId (paperLiftAbsShape absSig[i])`
   over the array, paired with the `regionAbs : Array AbsId` ids.
   Requires either (a) moving `liftAbsShape` / `liftAbsRoleEntry`
   into `Syntax.lean` (paper) so `Step.lean` can use them, or (b)
   adding paper-side equivalents.
2. **Replayer-side (AeneasCheck.LLBCSharp.Step)**: extend `stepEndAbs`
   to also call `st.absRegistry.erase absId` before returning, so
   `concretise st'.abs absId = none` matches `Ω.removeAbs abs`. No
   other replayer code reads erased abs entries (grep confirms only
   stepCall populates / stepEndAbs reads); G1-G4 expected green
   post-change.
3. **Valid.lean**: update `Valid.call` to existentially quantify
   per-AbsShape freshness premises (`absIdFresh` over each entry).
   Phase-D dispatch will discharge by reading `regionAbs` from the
   cert.
4. **Concretise/Defn.lean (& Lemmas)**: if step 1's helper moves to
   paper side, drop the soundness-side duplicate; add an
   `concretise_setAbs_fold` commute lemma if needed for C15.
5. **Re-prove C15** (M10.2o) with strengthened rule: chain
   `concretise_setLocal` ; `concretise_setAbs_fold` ; ...

This is roughly a 3-4 commit refactor (M10.0m for the paper-side
strengthening; M10.0n optional replayer mirror; M10.2o-revised for
the full C15). Defer ~2 sessions of Phase-C bulk if the user wants
to land C16-C17 with full absSig support; alternatively, continue
with C18-C22 (joins) in their empty-absSig restriction and revisit
the Phase-A revision later.

**Recommendation**: next session opens with M10.0m. Use the
`lean4:formalize` agent in a worktree, since the touch-set spans
3-4 files and the changes are mechanical (definition strengthening,
not deep proof work). Brief agent with the items above + the
specific `concretise.abs` shape that must match.

### Notes from Session 1

* **CI lane**: M10.0a scaffolds the Lake package only; adding a
  soundness CI job to `.github/workflows/ci.yml` is deferred. The
  existing ci.yml uses the nix flake which doesn't reference the
  checker by name either; the local 7-gate invocation from the
  execution prompt §0 is sufficient until a phase boundary.

* **`aeneas-lean-checker` mathlib-free**: confirmed — its lakefile
  has no `require` lines and the `RuntimeShim` library provides the
  `Aeneas.Std.*` surface that `GeneratedTests` needs without
  pulling in Mathlib. The soundness package's `require mathlib`
  doesn't leak.

* **`StepEventSound.lean` referenced nowhere**: confirmed by grep;
  no re-export shim needed in the checker. Plan §0.1 suggested one
  for safety; not required given the file was orphaned in the
  checker tree.

* **Namespace `AeneasSoundness.Soundness`**: M10.0a retargets the
  namespace from `AeneasCheck.Theorems` so the
  `AeneasSoundness.Soundness.stepEvent_sound` G5 golden line
  resolves without an alias hop.

* **`Place` not `«Place#»`**: M10.0b initially defined
  `abbrev «Place#» := Place` for paper-flavor naming, but the
  French-quote-escaped identifier (`«Place#»`) is unwieldy at every
  use site (Lean's parser doesn't accept `Place#` bare). Dropped
  the alias in M10.0f; downstream files just `open AeneasCheck.Raw
  (Place ProjElem)` and write `Place` / `ProjElem` directly.

* **Section comments inside inductive bodies**: Lean's grammar does
  not allow `/-! ... -/` between inductive constructors (the parser
  emits "expected 'lemma'" once it closes the inductive). Use
  `-- single-line` comments between constructor groups instead. Hit
  this twice in M10.0f and M10.0h.

* **Inline `-- comments` inside constructor types**: these are
  parsed as whitespace and don't break the parse, but the inductive
  may close at an unexpected place if the doc comment they precede
  isn't formally attached. Caught in M10.0f for `assign`'s
  multi-line body; resolved by removing the inline comments.

* **Doc-comment placement**: `/-- ... -/` immediately preceding a
  declaration attaches to it. If you insert other declarations
  between the doc comment and its intended target (M10.0h inserted
  `JoinEntryStep` + `JoinChain` between `LStep`'s docstring and
  `inductive LStep`), the parser errors. Convert to `/-! ... -/`
  section comments in that case.

* **`Place` field is `local_`**: `AeneasCheck.Raw.Place` calls its
  base local `local_` (trailing underscore to dodge the `local`
  keyword). Code under `LLBCSharpPaper/State.lean` and `Step.lean`
  uses `p.local_` directly.

* **`Event.matchArm` is 4-arity**: `Event.matchArm` takes
  `(scrutinee : SymExpr) (adtId variantId : Nat) (variantName :
  String)`. The plan's narrative treated it as `(adtId variantId)`
  only; the M10.0f draft had to be corrected.

* **`Multiset` notation `{x}`**: works at `Multiset
  (Role × LoanId)` for both `RegionAbs.singleton` and the
  `mutBorrows` constructor's role list; no special imports needed
  beyond `Mathlib.Data.Multiset.Basic`.

* **Per-entry join via auxiliary relation**: M10.0h introduces
  `JoinEntryStep` (6 constructors mirroring `JoinRule`) plus
  `JoinChain` (sequential composition over a list of `JoinEntry`).
  `LStep.join` then folds via `JoinChain Ω witnesses.toList Ω'`.
  Cleaner than 6 LStep variants that match `Event.join` with
  specific witness shapes.

* **Premise strength baseline**: every LStep constructor uses
  *baseline* premises that are expressible from Phase A's surface
  (place resolution, freshness counters, top-level
  `directLoanId` / `directBorrowId`). Deep-Val side conditions
  ("⊥/loan^{s,m} ∉ v"), parent-loan liveness, deref-chain
  consistency are deferred to Phase C; if a per-event lemma needs
  one, the Phase-C commit adds the corresponding `WellFormed`
  field. Per plan §3.4.

### Build & gate state at session end (tip afbe4624)

| Gate | Status | Notes |
|------|--------|-------|
| G1   | ✓     | vertical slice (M2-M8) ok |
| G2   | ✓     | 20/20 Direct fixture tests |
| G3   | ✓     | GeneratedTests 20-job build |
| G4   | ✓     | 89/0 sweep |
| G5   | ✓     | axioms.golden.txt matches |
| G6   | ✓     | no sorry under `Soundness/` |
| G7   | ✓     | warm 0.81 s, cold ~80 s (host has warm mathlib cache; budget < 30 min cold holds) |

### Notes from Session 3

* **Per-event lemma signature pattern**: The 4 sorry'd hint-bearing
  lemmas from M10.0k didn't carry `hRep` as an explicit hypothesis
  (they relied on the section's `variable (hRep : ...)`); but section
  variables don't auto-include for tactic-mode proofs that only
  reference them in tactics. Phase C lemmas added in Session 3
  carry `hRep` explicitly. The 4 still-sorry'd ones will need
  the same fix when they get proved (or we can add `include hRep`
  globally).

* **Place projection assumption (M10.2c)**: `stepMove` / `stepCopy`
  use `placeRootLocal` (the root local, ignoring projection); the
  paper-side `LStep.move` / `.copy` premise is `resolvePlace src
  = some v` which dives deep. To bridge, the soundness lemmas take
  three extra hypotheses: `src.projection = #[]`, `dst.projection
  = #[]`, and `∃ v, st.env[src.local_]? = some v`. These are
  Phase-D-dischargeable (cert emission discipline + future
  WellFormed strengthening).

* **`bumpSymValId` redefined as no-op (M10.2f)**: The paper-side
  `LStep.binop` (and several mutBorrow variants, plus call,
  symExpand) conclude with `(... ).bumpSymValId σ` which would
  bump `freshness.nextSymValId`. But `concretise` always sets
  `nextSymValId := 0` (the replayer doesn't track sym-value ids;
  `CertGen_faithful` carries cert-side monotonicity). The original
  `bumpSymValId` broke `concretise st' = Ω'`. Redefined as a no-op
  in `State.lean`. The `symValIdFresh σ` premise reduces to
  `0 ≤ σ` (trivially true). This refinement is load-bearing for
  the still-pending C8-C10, C14, C16, and C19.

* **`concretise_addLoan` is the next blocker** (M10.2g/C7 onwards):
  Every event that adds a loan (stepSharedBorrow / stepMutBorrow
  family / etc.) needs this commute lemma to discharge
  `concretise st' = Ω'`. The underlying sub-lemma is
  `maxKeyPlusOne (m.insert k v) = max (maxKeyPlusOne m) (k+1)`
  for `Std.HashMap`. Under the freshness premise
  (`maxKeyPlusOne m ≤ k`), the RHS simplifies to `k+1`, so the
  needed sub-lemma is `maxKeyPlusOne_insert_fresh`:
  ```
  maxKeyPlusOne (m.insert k v) = k + 1
    when maxKeyPlusOne m ≤ k
  ```
  Proof skeleton: `Std.HashMap.fold_eq_foldl_toList` →
  `Std.HashMap.toList_insert_perm` → `List.Perm.foldl_eq` with
  `RightCommutative max` instance → simp the result foldl over
  the unfiltered `(k,v) :: m.toList` (the freshness premise makes
  `filter (¬k == ·.1) m.toList = m.toList`). All Mathlib pieces
  exist; the proof is ~30-50 LOC. **Next session: dispatch
  `lean4:sorry-filler-deep` for this lemma in a worktree** while
  continuing other planning.

* **C7 specifically (stepSharedBorrow)**: Once `concretise_addLoan`
  closes, the proof should be straightforward — `stepSharedBorrow`
  on success returns `st.addLoan loan inner .shared` (no env
  mutation); `LStep.sharedBorrow` conclusion is
  `(Ω.bumpLoanId ℓ).bumpSymValId σ` which under the no-op
  `bumpSymValId` = `Ω.bumpLoanId ℓ` — exactly what
  `concretise_addLoan` gives us.

### Notes from Session 4

* **`if_pos` / `if_neg` over `simp only [hC, if_true/false]`**: The
  prior pattern (used in M10.2f stepBinop) of `simp only [hC,
  ite_true]` doesn't reduce `if (false = true) then ... else ...`
  because `if_false` is keyed on `if False then a else b`, not on
  the propositional `false = true`. Using `rw [if_pos hC]` /
  `rw [if_neg hC]` directly handles both the Bool-style guard
  (`st.loans.contains loan = true`) and the Prop-style guard
  (`place.local_ ≥ st.numLocals`). Cleaner and uniform across
  guards.

* **Defining `hFreshSet` for setLocal-then-addLoan**: In C8/C10
  the proof reaches `concretise ((st.setLocal _ _).addLoan loan
  _ _)`. To apply `concretise_addLoan`, the freshness premise must
  match the *intermediate* `(st.setLocal _ _).loans`. Since
  setLocal only mutates `env`, `(st.setLocal _ _).loans = st.loans`
  by `rfl`, but Lean's elaborator won't always unify with the
  underscore-filled lemma instance. Workaround: introduce
  `have hFreshSet : maxKeyPlusOne (st.setLocal _ _).loans ≤ loan
  := hLoanFresh` (the coercion is definitional), then pass
  `hFreshSet` to `concretise_addLoan`. Adds ~3 LOC per such
  lemma; worth it for clarity.

* **`concretise_takeLoan` is potentially unsound as stated**:
  The lemma claims `concretise st' = concretise st` after
  `takeLoan b = some (_, st')`. But `takeLoan` calls
  `loans.erase b`, and `maxKeyPlusOne (loans.erase b) ≤
  maxKeyPlusOne loans` (can strictly decrease if `b` was the
  max key). So `(concretise st').freshness.nextLoanId` can
  strictly *decrease*, violating the literal equality. Phase
  C's first endBorrow lemma (C11) will surface this. Likely
  fix: redefine `concretise`'s `nextLoanId` to be an *upper
  bound* counter that never shrinks (e.g., carry an explicit
  high-water mark in `SymState`, or use `max (maxKeyPlusOne
  st.loans) (cert-supplied bound)`). Defer the design choice
  to when C11 needs it — may also need a `WellFormedProgram`
  strengthening + cert-side monotonicity premise.

* **Worktree auto-merge**: The `lean4:sorry-filler-deep` agent
  with `isolation: "worktree"` was supposed to commit on a
  separate branch (`worktree-agent-<id>`); the orchestrator was
  to fast-forward merge. In practice, the harness *auto-merged*
  the worktree's clean commit into `aeneas-lean-certificate`
  directly (worktree removed; tip moved to `9be15ea7`). No
  intervention needed; verify via `git log` after the agent
  returns rather than running the merge protocol manually.

### Notes from Session 7

* **Path 1 vs Path 2 decision**: Path 1 (per-entry) chosen, with a
  pragmatic deviation from plan §3.2's 5-commit cadence. The plan
  envisioned per-entry lemmas at ~250 LOC each (bundling replayer-
  correspondence per entry). In reality, the replayer's `stepJoin`
  is wholesale (no per-entry incremental updates exist), so per-
  entry lemmas can only be paper-side constructor wrappers — one-
  line `JoinEntryStep.<rule> <freshness-or-existence-premise>`
  facts. Five such facts collapsed naturally into one M10.2r commit
  (~50 LOC total). The "5-wide parallel batch" §3.3 suggested was
  not worth the orchestration overhead for trivial lemmas.

* **`JoinChain.nil` qualification**: `JoinChain` is not in the
  `open AeneasSoundness.LLBCSharpPaper (LLBCState LStep Valid)`
  list at the top of StepEventSound.lean, so the literal
  `JoinChain.nil` is unknown. Fully-qualified `LLBCSharpPaper.
  JoinChain.nil` works. (`LStep.join` resolves because `LStep` is
  in the opened list.) If future join-cluster lemmas need richer
  chain manipulation, consider broadening the `open` line.

* **C23 empty-witnesses subset trivially closes**: Once
  `witnesses = #[]`, the `Valid.join` existential reduces to
  `⟨Ω, JoinChain.nil⟩`, `LStep.join` reduces to
  `LStep.join JoinChain.nil`, and `concretise st' = Ω` collapses
  to `hStShape.trans hRep` for the Phase-D-dischargeable hypothesis
  `hStShape : concretise st' = concretise st`. 5-line proof body.
  Total lemma + docstring: 47 LOC of insertions.

* **"Fresh abs" gap is real, not theoretical**: confirmed by
  inspection of `stepJoin` (Step.lean:506-554). The replayer never
  touches `absRegistry`. Any non-`joinSame`/`joinVar`/`joinBottom*`
  witness would force the chain's terminal Ω' to include an abs
  entry that `concretise st'` lacks. The M9.8 schema bump (option
  A in the blocker write-up) is the cleanest fix; option (B) drifts
  trust; option (C) restricts the corollary. See Blockers section.

### Notes from Session 6

* **`absIdHwm` mirrors `loanIdHwm`** — same pattern as M10.1g but
  for abs ids. Bumped by `stepCall`'s fold (via the new
  `SymState.addAbsShape` helper) and untouched by `stepEndAbs`'s
  `removeAbsShape` (which only `absRegistry.erase`'s, not the
  HWM). `concretise.freshness.nextAbsId` retargets from
  `maxKeyPlusOne st.absRegistry` to `st.absIdHwm`, making the
  M10.1i `concretise_addAbsShape` and `concretise_removeAbsShape`
  commutes both unconditional. Two new bridging hashmap lemmas
  `liftAbsRegistry_insert` / `liftAbsRegistry_erase` mirror
  `liftEnv_insert`.

* **`liftAbsShape` moved paper-side** — into
  `LLBCSharpPaper/Syntax.lean`, alongside `Role` /  `RegionAbs`.
  The `AeneasCheck.Raw.CertEvent` import was added to `Syntax.lean`
  to bring `AbsShape` into scope (no cycle: `CertEvent` only
  depends on `Places` / `Literal`). Soundness-side `Concretise/Defn.lean`
  re-`open`s the moved names so existing call sites work. The one
  `Concretise.liftAbsShape`-qualified reference in
  `StepEventSound.lean` (C8b mutBorrow_inAbsReborrow) updated to
  `LLBCSharpPaper.liftAbsShape`.

* **`LStep.call` post-state order matches `stepCall`** —
  fold-then-setLocal-then-bumpSym. The fold step is
  `installAbsShapePaper Ω' shape := (Ω'.setAbs shape.absId
  (liftAbsShape shape)).bumpAbsId shape.absId`. No freshness
  premise needed on `absSig` — the `max`-based bump commutes with
  the lift even if `shape.absId` is non-fresh (overwrites are
  fine; `bumpAbsId` is idempotent on already-bumped HWMs).
  `Valid.call` stays as just `∃ σ, Ω.symValIdFresh σ` (no extra
  premise).

* **`concretise_foldl_addAbsShape` lemma** — fold-commute that
  pushes `concretise` through `absSig.foldl SymState.addAbsShape`.
  Proved by `rw [Array.foldl_toList, Array.foldl_toList]` then
  induction on `absSig.toList`. The cons case is one `ih` + one
  `concretise_addAbsShape` hop + `rfl`. ~10 LOC.

* **C16 stepEndAbs uses the result-shape pattern from C11-C13**
  — but with one extra layer: the replayer's `stepEndAbs` has two
  preamble for-loops (loan-erase + token-clear) followed by
  `removeAbsShape absId`. The `(stPre, hConcPre, hShape)` triple
  folds the preambles into a single `concretise`-preserving step.
  The proof itself is one `congrArg (·.removeAbs absId)
  (hConcPre.trans hRep)` once `concretise_removeAbsShape` has
  pushed `concretise` past the removeAbsShape.

* **C17 stepSymExpandMutBorrow needs the η-fold trick** — after
  empty-array `for` loops, `hStep` carries the literal record
  `{ env := st.env, loans := st.loans, ... }` which is
  *definitionally* equal to `st` but `simp` won't reduce it.
  Workaround: one `have hEtaSt ... := rfl` line proves the record
  equality, then `rw [hEtaSt] at hStep` cleans it up. Worth
  remembering for any future lemma that hits empty-for-loop
  reduction. This same shape may also surface in stepEndAbs if
  we ever try to drop the result-shape hypothesis.

* **C18-C22 (joins) deferred to Session 7** — the replayer's
  `stepJoin` (lines 506-554 of `AeneasCheck/LLBCSharp/Step.lean`)
  does a *wholesale* replacement of `st.env` from `result.env`
  and prunes `st.loans` against `result.liveLoans`. The paper-side
  `LStep.join` chains per-entry `JoinEntryStep` rules via
  `JoinChain`. These two semantics don't align trivially — the
  replayer doesn't compute per-entry transitions, it just
  installs the post-state directly. Plan §3.3 recommends
  factoring C18-C22 into sibling files under
  `AeneasSoundness/Soundness/JoinLemmas/Join<Rule>.lean` so they
  can parallelise. C20 (`JoinMutBorrows`) is the known-hard one
  (plan §6.1 #5 — likely M9.8 trigger; the paper's
  `Collapse-Dup-MutBorrow` introduces a fresh abs the cert may
  not name).

### Notes from Session 5

* **loanIdHwm redesign**: the Session 4 caveat ("`concretise_takeLoan`
  is potentially unsound as stated") forced a SymState shape change.
  `SymState` gains a monotone `loanIdHwm : Nat := 0` field bumped by
  `addLoan` and never touched by `takeLoan` / `loans.erase`. The
  replayer's checker logic never reads this field — it exists solely
  for soundness. `concretise.freshness.nextLoanId` retargets at
  `st.loanIdHwm` (was `maxKeyPlusOne st.loans`). Two consequences:
  `concretise_takeLoan` closes (no field changes either side);
  `concretise_addLoan` becomes *unconditional* (the LHS `loanIdHwm
  := max old (b+1)` matches the RHS `bumpLoanId b := max old (b+1)`
  by `rfl`). The freshness premise on C7-C10 lemmas renames from
  `maxKeyPlusOne st.loans ≤ loan` to `st.loanIdHwm ≤ loan`; uses of
  `concretise_addLoan _ _ _ _ hFresh` drop the explicit witness.
  Sym-arg `absIdHwm` not yet needed (no event in the current LStep
  surface erases from absRegistry; if endAbs lemma ever does, same
  fix applies).

* **End-borrow trio (C11-C13) contract**: rather than unroll the
  replayer's `for ... in st.env.toList` env-scan loop, each lemma
  takes a Phase-D-dischargeable *result-shape* hypothesis pinning
  st' directly:
  - C11 (.direct / .lazyExpand): `(x, v, stTake, hShape : stepEvent
    ... = .ok (stTake.setLocal x v))` + `hHolder : Ω.ctx x =
    some (.mutLoan loan)`.
  - C12 (.reborrow): `(stTake, hShape : stepEvent ... = .ok stTake)`.
  - C13 (.shared): same as C12.
  Phase D discharges `hShape` and `hHolder` from the `LoanTokenInvariant`
  uniqueness fact (plan §3.4 strengthening). This is the same
  "Phase-D-dischargeable" pattern used by C2-C10 for place-projection
  and env-existence hypotheses.

* **`LocalId` not in scope** at `Soundness/StepEventSound.lean`:
  `LocalId := Nat` lives in `LLBCSharpPaper`, but the file's
  `open AeneasSoundness.LLBCSharpPaper (LLBCState LStep Valid)` is
  selective — `LocalId` is not opened. Lean auto-binds the
  identifier as a polymorphic type variable (`Sort u_1`), which
  triggers a confusing "of sort Sort u_1 but is expected to have type
  ... of sort Type" error. Workaround: use `Nat` directly in
  parameter signatures (matches the pattern of `place.local_ : Nat`
  in the mutBorrow lemmas). If future lemmas need many `LocalId`
  / `AbsId` parameters, broaden the `open` line.

### Session boundaries

- **Session 1: 2026-05-17, commits M10.0a → M10.0i** (9 commits).
  Started from the post-M9.7 baseline; the four axioms in
  `StepEventSound.lean` are still in place but the paper-side
  surface they will be replaced by is now ported (Syntax, State,
  Program helpers, WellFormed, all 24+4 LStep constructors).

- **Session 2: 2026-05-17, commits M10.0j → M10.1e** (8 commits).
- **Session 3: 2026-05-17, commits M10.2a → M10.2f** (6 commits).
  Phases A *and* B closed. `Valid` predicate ported; `StepEventSound.lean`
  migrated to real types; janitorial fixes from the boundary review
  applied (M10.0l). `concretise` fully defined with env/absRegistry/
  freshness; inversion + setLocal commute lemmas in place. Two
  deferred sorrys: `concretise_addLoan` / `concretise_takeLoan`
  commute lemmas (hashmap-fold equality is the missing step;
  Phase C closes when first lemma needs them — likely M10.2h
  `stepMutBorrow_direct_sound`). G6 still exempt
  (Phase C starts at commit #17; we're at #16).

- **Session 4: 2026-05-17, commits M10.1f → M10.2j** (3 commits).
  C7–C10 closed. `concretise_addLoan` unblocked via a deep agent
  that landed `maxKeyPlusOne_insert_fresh` (~140 LOC helper block
  in `Concretise/Defn.lean`). The agent's worktree auto-merged
  cleanly. C8–C10 batched in one commit; they share the if-guard
  reduction (`rw [if_pos hC] at h; cases h` for the loan-conflict
  branch and the bounds-check branch), the `loanIdFresh`
  derivation from `hLoanFresh : maxKeyPlusOne st.loans ≤ loan`,
  and the `concretise_setLocal ; concretise_addLoan` chain.
  10/23 Phase-C done. Next: C11–C13 (endBorrow trio) which
  needs `concretise_takeLoan` — currently sorry'd and possibly
  unsound as stated (`erase` can shrink `maxKeyPlusOne`; see
  Session 4 notes).

- **Session 5: 2026-05-17, commits M10.1g → M10.2o** (5 commits).
  loanIdHwm redesign (M10.1g checker addition + M10.1h soundness
  retarget) resolved the Session 4 caveat; `concretise_takeLoan`
  closes cleanly, `concretise_addLoan` becomes unconditional. C11-C13
  (endBorrow trio) batched into M10.2k-m via Phase-D-dischargeable
  result-shape hypotheses, avoiding the env-scan loop unroll.
  C14 (stepReborrow) closes with `hParentInHwm` no-op trick on
  pre-add-parent branch. C15 partial (empty-absSig only); the
  non-empty case surfaced a Phase-A surface gap in LStep.call that
  also blocks C16 — see Blockers section for the M10.0m plan.
  15/23 Phase-C done (one partial). Tip 922c0bb0.

  Gate state at session end (922c0bb0): G1 ✓ vertical slice;
  G2 ✓ 20/20 Direct fixtures; G3 ✓ GeneratedTests; G4 ✓ 89/0 sweep;
  G5 ✓ axioms.golden.txt matches (`propext, sorryAx, Quot.sound`);
  G6 ✓ only expected sorrys (`stepJoin_witnessed_sound` C18-C22 +
  top-level `stepEvent_sound` D); G7 ✓ warm 0.8s.

- **Session 7: 2026-05-17, commits M10.2r → M10.2s** (2 commits).
  Phase C now at 23/23 lemmas declared. M10.2r bundles C18-C22 as
  six paper-side `JoinEntryStep` constructor wrappers in a new
  `Soundness/JoinLemmas.lean` (deviates from plan's 5-commit
  cadence; see Session 7 notes). M10.2s closes C23
  (`stepJoin_witnessed_sound`) in the `witnesses = #[]` subset;
  the general case is the campaign's deepest open issue, escalated
  to user as an M9.8 schema-bump candidate (see Blockers section).
  Tip 49ef3650.

  Gate state at session end (49ef3650): G1 ✓ vertical slice;
  G2 ✓ 20/20 Direct fixtures; G3 ✓ GeneratedTests; G4 ✓ 89/0 sweep;
  G5 ✓ axioms.golden.txt matches (`propext, sorryAx, Quot.sound`);
  G6 ✓ only one expected sorry remaining (top-level
  `stepEvent_sound`, the C23 sorry is gone in the empty-witnesses
  subset); G7 ✓ warm 0.77s.

- **Session 6: 2026-05-17, commits M10.1i → M10.2q** (4 commits).
  Phase-A inflection point closed. M10.1i adds `SymState.absIdHwm`
  (monotone mirror of `loanIdHwm`), retargets
  `concretise.freshness.nextAbsId` at it, extends `stepEndAbs` to
  erase the absRegistry entry, and moves `liftAbsShape` /
  `liftAbsRoleEntry` from the soundness-side `Concretise/Defn.lean`
  into the paper-side `LLBCSharpPaper/Syntax.lean`. M10.0m strengthens
  `LStep.call`'s post-state to fold `setAbs ; bumpAbsId` over `absSig`
  (fold-then-setLocal-then-bumpSym order, matching the replayer); C15
  is re-proved unrestricted (M10.2o-revised) in the same commit. M10.2p
  (C16 stepEndAbs) and M10.2q (C17 stepSymExpandMutBorrow,
  no-substitution subset) follow the same Phase-D-dischargeable
  result-shape pattern as C11-C13. 17/23 Phase-C done; only C18-C22
  joins remain. Tip debe8ec3.

  Gate state at session end (debe8ec3): G1 ✓ vertical slice;
  G2 ✓ 20/20 Direct fixtures; G3 ✓ GeneratedTests; G4 ✓ 89/0 sweep;
  G5 ✓ axioms.golden.txt matches (`propext, sorryAx, Quot.sound`);
  G6 ✓ only expected sorrys (`stepJoin_witnessed_sound` C18-C22 +
  top-level `stepEvent_sound` D); G7 ✓ warm ~1.5s.

### Notes from Session 2

* **G6 exemption clarified**: Plan §10.1 line 697 spells out that
  G6 runs "After every Phase-C/D/E/F/G commit (i.e., commits #17
  onward)". Per-event `sorry`s in `StepEventSound.lean` (M10.0k)
  + addLoan/takeLoan commute sorrys (M10.1e) are *not* G6
  violations — Phase C closes them.

* **`.proj` events**: Per `Valid.lean`, `Valid (.proj _ _ _) :=
  False`. Vacuously consistent with the iff smoke lemma. Phase D's
  case-split needs `False.elim` (or refute via the replayer's
  rejection of proj events at `Replay.lean:50`). No Phase-A test
  fixture emits `.proj` (confirmed by review).

* **Phase-A boundary review GREEN**: doctor + review agents
  returned 7 nits + 5 design observations; 3 nits applied as
  M10.0l. The two "soft block" caveats — `LStep.assign`
  unconstrained `v` and `endBorrow` constructor dispatch — were
  documented as deliberate design intents in `Step.lean` rather
  than fixed in the constructors; they reflect the existential-v
  / replayer-LoanKind-driven dispatch contract for Phase D.

* **Open Phase-C contracts** (capture before C1 lands):
  - The "Phase D delegation contract" for `endBorrow` /
    `mutBorrow` constructor dispatch is recorded in
    `Step.lean`'s top-level docstring (M10.0l). When Phase C
    lemmas commit to a signature, the replayer-side LoanKind /
    MutBorrowKind drives the LStep constructor choice.
  - `concretise_addLoan` / `concretise_takeLoan` commute lemmas
    are sorry'd in `Concretise/Lemmas.lean` (M10.1e). The
    hashmap-fold equality
    `maxKeyPlusOne (m.insert k v) = max (maxKeyPlusOne m) (k+1)`
    is the load-bearing missing step; close it before M10.2h
    (`stepMutBorrow_direct_sound`).

* **JoinDeterminisation axiom NOT introduced yet**: per plan
  §11.1 #1, this was an anticipated Phase-A axiom. The Phase-A
  ports got through without needing it (the cert's `JoinEntry`
  array is the deterministic ordering). May land if a Phase-C
  join lemma needs it.
