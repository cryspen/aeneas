## Plan: M10 LLBC# Soundness
## Started: 2026-05-17
## Last action: M10.0a in flight — scaffolding aeneas-lean-soundness Lake package (Mathlib pin v4.30.0-rc2). Cold build running.
## Phase: A
## Next commit: M10.0a Soundness: scaffold aeneas-lean-soundness Lake package + Mathlib pin

### Completed commits

(none yet)

### In-flight
- [ ] M10.0a — scaffold the Lake package, move `StepEventSound.lean` from
      `aeneas-lean-checker/AeneasCheck/Theorems/` to
      `aeneas-lean-soundness/AeneasSoundness/Soundness/`, namespace
      retargeted from `AeneasCheck.Theorems` to `AeneasSoundness.Soundness`.
      Status: files written; cold mathlib build in progress; waiting on G7
      baseline + axioms.golden.txt capture.

### Trusted-base inventory (live)

The four "paper-theorem placeholder" axioms below are introduced by
Phase A (M10.0e/h) and replaced (one-by-one) by Phase G ports if/when
Phase G is undertaken. Until then they remain as the *expected* trusted
extension; G5 enforces no others.

| Axiom | Reason | Replaced by |
|---|---|---|
| `CertGen_faithful` | OCaml-side honesty (broadened post-M9.7 to cover both event trace and embedded `cc.llbcProgram` faithfulness) | (forever trusted) |
| `paper_thm_3_1_confluence` | Phase A placeholder; paper Thm 3.1 | Phase G commit M10.6d (optional) |
| `paper_thm_3_3_pl_refines`  | Phase A placeholder; paper Thm 3.3 | Phase G commit M10.6e (optional) |
| `paper_thm_4_1_safe`        | Phase A placeholder; paper Thm 4.1 | Phase G commit M10.6f (optional) |
| `paper_thm_4_2_init`        | Phase A placeholder; paper Thm 4.2 | Phase G commit M10.6g (optional) |

### Blockers escalated to user
(none)

### Notes

- **CI lane**: M10.0a scaffolds the Lake package only; adding a
  soundness CI job to `.github/workflows/ci.yml` is deferred (the
  existing checker is not directly addressed by the workflow either —
  it's built via the nix flake). Filed as a follow-up; the local gate
  invocations from the execution prompt §0 are sufficient until the
  campaign hits a phase boundary.
- **`aeneas-lean-checker` is mathlib-free**: confirmed by reading the
  checker's lakefile (no `require mathlib`, no `require aeneas`; the
  manifest's `aeneas` entry is stale and unused — the `RuntimeShim`
  library provides the `Aeneas.Std.*` surface the `GeneratedTests` lib
  needs without pulling in Mathlib). Soundness pulls Mathlib only on
  its own side; checker CI stays at ~1 s.
- **`StepEventSound.lean` referenced nowhere else**: confirmed by grep;
  no re-export shim needed in the checker. Plan §0.1 suggested one for
  safety; not required given the file was orphaned in the checker tree.
- **Namespace change**: M10.0a renames the namespace from
  `AeneasCheck.Theorems` to `AeneasSoundness.Soundness` so the
  `#print axioms AeneasSoundness.Soundness.stepEvent_sound` in
  `tests/AxiomCheck.lean` (G5) resolves without an alias hop.
- **Lake `require` shape**: soundness lakefile uses
  `require «aeneas-lean-checker» from "../aeneas-lean-checker"` (path
  dep, no git). The checker has no transitive deps so this builds a
  thin slice — only the modules `StepEventSound.lean` imports
  (`AeneasCheck.LLBCSharp.Replay` and its dependencies) compile.

### Session boundaries
- Session 1: 2026-05-17, commits M10.0a → (in flight)
