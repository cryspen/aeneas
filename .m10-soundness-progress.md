## Plan: M10 LLBC# Soundness
## Started: 2026-05-17
## Last action: Session 17. Drop `symExpandMutBorrow` from `CertGen_faithful` (commit M10.x.8). Pre-flight scan: 418/423 EvSymExpandMutBorrow fixtures have non-empty `substLocals` — the previous "empty subset" axiom was false on nearly the entire corpus, a real soundness hole. Paper-rule strengthening: extended `LStep.symExpandMutBorrow`'s post-state to `((substLocals.foldl substLocalOne Ω).bumpLoanId bid).bumpSymValId innerSv` (was `(Ω.bumpLoanId bid).bumpSymValId innerSv`). substLoans is concretise-no-op (replayer's `loans` field invisible to concretise) so doesn't enter the paper post-state. Replayer-side: added M10.x.8 HWM reject path (parallel to M10.x.2 for stepMutBorrow) discharging `st.loanIdHwm ≤ bid`; refactored substLocals/substLoans `for+mut` blocks into explicit `Array.foldl` over new `substLocalsOne` and `substLoansOne` helpers. New paper-side `LLBCState.substLocalOne` primitive mirrors `substLocalsOne`. Five new commute lemmas in Concretise/Lemmas.lean (~140 LOC): `concretise_env_substLocalsOne` per-step + `concretise_env_substLocalsOne_foldl_aux` private list induction + `concretise_env_substLocalsOne_foldl` fold-form; `concretise_loans_substLoansOne` per-step (no-op) + `concretise_loans_substLoansOne_foldl_aux` (no-op fold private) + `concretise_loans_substLoansOne_foldl` (no-op fold public). Plus `concretise_stepSymExpandMutBorrowBody_eq` private helper in StepEventSound.lean. `stepSymExpandMutBorrow_sound` dropped all 4 hypotheses (`hSubstLocalsEmpty` / `hSubstLoansEmpty` / `hBidNotInLoans` / `hBidFresh`). Dispatcher drops the `CertGen_faithful.symExpandMutBorrow` `obtain`. `tests/axioms.golden.txt`: −1 line. TCB shrinks 3 → 2 `CertGen_faithful.*` entries. G5 BYTE-MATCH; G6 zero sorrys under `Soundness/`; G7 warm `lake build` ~0.78s. Direct/Reborrows + ListBasic hand-verified pass.
## Phase: D complete. E1 (paper Fig. 10) complete. M10.x.0-8 complete (TCB 16 → 2). Next: M10.x.9 — cert v6 consumer for `endBorrow_direct_witness` (low risk, mechanical: checker strengthens `stepEndBorrow.direct/lazyExpand` to require `restore.holderLocal = some x`).

## Previous-step record (for context): Session 17. Drop `loopInv` from `CertGen_faithful` (commit M10.x.7). Strengthen paper `LStep.loopInv` post-state with `loanRegistry.foldl bumpLoanId`; thread `HwmInvariant` through `stepEvent_sound`. Pre-flight scan found 32/170 EvLoopInv events with non-empty loanRegistry, ruling out the "reject non-empty" alternative (would have regressed G4). Same M10.x.6 strengthening pattern; the previous `Ω → Ω` no-op rule was strictly weaker than the replayer's conditional `addLoan` fold, requiring `loanRegistry = #[]` via CertGen_faithful to close. M10.x.7 closes honestly: paper rule's per-entry effect is `Ω'.bumpLoanId entry.1` (unconditional bump), which on the replayer-side maps via `if !st.loans.contains b then addLoan else id`. The skip-branch (`b ∈ st.loans` → no replayer effect, but paper bumps) coincides only if `b < st.loanIdHwm`, i.e. the HwmInvariant. M10.x.1's `HwmInvariant` infrastructure scaffolded for exactly this; M10.x.7 threads it through `stepLoopInv_sound` and (uniformly) `stepEvent_sound`. The other 16 events ignore the hypothesis. Added 3 commute lemmas in `Concretise/Lemmas.lean`: `concretise_loopInvRegisterLoan` (per-entry, HwmInvariant); `concretise_loopInvRegisterLoan_foldl_aux` (private list induction); `concretise_loopInvRegisterLoan_foldl` (public fold-form). Replayer-side `stepLoopInv` was Lean-only-refactored from `for + mut` to `Array.foldl` over a new `loopInvRegisterLoan` helper (parallel to M10.x.6's pattern). G5 BYTE-MATCH; G6 zero sorrys under `Soundness/`; G7 warm `lake build` ~0.87s. Direct/Loops hand-verified pass.
## Phase: D complete. E1 (paper Fig. 10) complete. M10.x.0-7 complete (TCB 16 → 3). Next: M10.x.8 — paper-side `LStep.symExpandMutBorrow` extension (the substitution-bearing case).

## Previous-step record (for context): Session 17. Drop `endAbs` from `CertGen_faithful` (commit M10.x.6). The "ugly triple" axiom existentially bound an `stPre : SymState` such that `concretise stPre = concretise st` and `stepEvent st (.endAbs ...) = .ok (stPre.removeAbsShape absId)`. Investigation revealed this existence claim is FALSE in general: the replayer's token-clear loop rewrites `env[l] = .mutLoan _` to `.bottom`, and `liftEnv` reflects this distinction (`liftVal .mutLoan b = .mutLoan b` vs `liftVal .bottom = .bottom`). So `concretise stPre ≠ concretise st` on every fixture that exercises token-clearing — the axiom was load-bearing on an unsatisfiable existential. M10.x.6 fixes the gap honestly through **paper-rule strengthening** (the M10.x.7/M10.x.8 pattern): the paper rule's post-state was extended with a `tokenClearLocals.foldl clearMutLoanToken` mirror of the replayer's env-clearing. Path-axis choice: this path is the audit's recommendation (factor stepEndAbs into stepEndAbsPre + stepEndAbsRemove + concretise commute) — but the commute claim the audit named (`concretise stPre = concretise st`) was the false existential, so the actual fix is the strengthening, not the factoring. Implementation: (a) checker-side, refactored `stepEndAbs` into `stepEndAbsValidate >>= stepEndAbsBody` (Lean-only; behavior preserved); the body uses explicit `Array.foldl` over new helpers `loansEraseIfPresent` and `tokenClearOne`. (b) paper-side, added `LLBCState.clearMutLoanToken` primitive + extended `LStep.endAbs` post-state to `tokenClearLocals.foldl clearMutLoanToken (Ω.removeAbs abs)`; dropped the vestigial `{r : RegionAbs}` binder. (c) soundness-side, added four commute lemmas in `Concretise/Lemmas.lean`: `concretise_loansEraseIfPresent` (no-op), `concretise_foldl_loansEraseIfPresent` (no-op fold), `concretise_env_tokenClearOne` (per-step mirror), `concretise_env_foldl_tokenClearOne` (fold mirror), `clearMutLoanToken_removeAbs_commute` (distinct-field commute), `foldl_clearMutLoanToken_removeAbs_commute` (fold-commute). (d) `stepEndAbs_sound` reduced to: `cases` on validation result, then `concretise_stepEndAbsBody_eq` chains the four commutes to match the paper-side post-state. `Valid (.endAbs _ _ _ _)` collapses to `True`. `tests/axioms.golden.txt`: −1 line. TCB shrinks 5 → 4 `CertGen_faithful.*` entries. Checker behavior preserved (G2 Direct: Aggregates / Calls / ListBasic / Reborrows hand-verified pass, all exercise EndAbs); G3 GeneratedTests builds. G5 BYTE-MATCH; G6 zero sorrys under `Soundness/`; G7 warm `lake build` ~0.80s.
## Phase: D complete. E1 (paper Fig. 10) complete. Tier-1 axiom cleanup complete. Cert v6 schema-plumbing commit (M10.x.0) complete. M10.x.1 Lean invariants complete. M10.x.2 replayer-strengthening complete. M10.x.3 drop move + copy complete. M10.x.4 drop sharedBorrow + mutBorrow_{direct,loopOwned} + reborrow complete. M10.x.5 drop mutBorrow_inAbsReborrow complete. M10.x.6 drop endAbs complete (TCB 5 → 4). Next: M10.x.7 — paper-side `LStep.loopInv` extension.
## Next commit: M10.x.7 — Drop `loopInv` from `CertGen_faithful` by extending paper `LStep.loopInv`'s post-state with the loanRegistry fold (paper §5.2 — install `.reborrow` loans for input borrows the body re-derives). Approach mirrors M10.x.6's endAbs strengthening: define paper-side `LLBCState.loopInvFold` (or similar), prove the per-step commute, extend the LStep rule. TCB 4 → 3.

### Completed commits (Session 1 — 2026-05-17)
- [x] M10.0a Soundness: scaffold aeneas-lean-soundness Lake package + Mathlib pin — c0a0413a — 2026-05-17
- [x] M10.0b Soundness: port LLBCSharpPaper.Syntax (Val#, Place#, RegionAbs) — d0355236 — 2026-05-17
- [x] M10.0c Soundness: port LLBCSharpPaper.State (LLBCState, ctx, abs maps) — 4ad9f22d — 2026-05-17
- [x] M10.0d Soundness: define LLBCSharpPaper.Program helpers (lookupFunDecl etc.) — 867a2cae — 2026-05-17
- [x] M10.0e Soundness: port LLBCSharpPaper.WellFormed (+ WellFormedProgram) — 3e1bbce9 — 2026-05-17
- [x] M10.0f Soundness: port LStep Fig. 3 rules — bd1cf78e — 2026-05-17
- [x] M10.0g Soundness: port LStep Fig. 7+8 rules (reborrow, call, endAbs, symExpandMutBorrow) — 446605bb — 2026-05-17
- [x] M10.0h Soundness: port LStep Fig. 11 join rules — d45180d0 — 2026-05-17
- [x] M10.0i Soundness: port LStep §5.2 loop rule (loopInv) — afbe4624 — 2026-05-17

### Completed commits (Session 3 — 2026-05-17)
- [x] M10.2a Soundness: stepPanic / stepRetn soundness (trivial passthroughs) — cf85dc4d — 2026-05-17
- [x] M10.2b Soundness: stepMatchArm / stepLoopEnd soundness (no-op markers) — a09e34c8 — 2026-05-17
- [x] M10.2c Soundness: stepMove / stepCopy soundness — f7b3ee49 — 2026-05-17
- [x] M10.2d Soundness: stepAssign soundness — e1c834c5 — 2026-05-17
- [x] M10.2e Soundness: stepAssert soundness (true/false branches) — a25d2f06 — 2026-05-17
- [x] M10.2f Soundness: stepBinop soundness + bumpSymValId redefined as no-op — a7d4b2a3 — 2026-05-17

### Completed commits (Session 4 — 2026-05-17)
- [x] M10.1f Soundness: close concretise_addLoan via maxKeyPlusOne_insert_fresh — 9be15ea7 — 2026-05-17 (sorry-filler-deep agent in worktree, auto-merged)
- [x] M10.2g Soundness: stepSharedBorrow soundness (C7) — f645fe49 — 2026-05-17
- [x] M10.2h-j Soundness: stepMutBorrow direct/inAbsReborrow/loopOwned (C8-C10) — 3e5e3318 — 2026-05-17 (batched: three M9.6 hint variants share guards, freshness, and the concretise_addLoan chain)

### Completed commits (Session 5 — 2026-05-17)
- [x] M10.1g Soundness: add loanIdHwm to SymState (monotone HWM for nextLoanId) — 8c6e54ff — 2026-05-17 (checker-side addition; replayer behavior unchanged)
- [x] M10.1h Soundness: retarget concretise.nextLoanId at loanIdHwm; close concretise_takeLoan — 6f2d4f4d — 2026-05-17 (concretise_addLoan now unconditional)
- [x] M10.2k-m Soundness: stepEndBorrow direct/reborrow/shared (C11-C13) — ee95913c — 2026-05-17 (batched: shared takeLoan-commute discharge + Phase-D-dischargeable result-shape contract)
- [x] M10.2n Soundness: stepReborrow soundness (C14) — 4a5415af — 2026-05-17 (case-splits on `loans.contains parent`; both branches fold to `Ω.bumpLoanId child` via `hParentInHwm` no-op argument)
- [x] M10.2o Soundness: stepCall soundness, empty-absSig subset (C15a) — 922c0bb0 — 2026-05-17 (Phase-A surface gap: LStep.call doesn't install absSig in post-state; restricted to absSig = #[] case)

### Completed commits (Session 7 — 2026-05-17)
- [x] M10.2r Soundness: bundle C18-C22 per-entry JoinEntryStep lemmas — 712befaa — 2026-05-17 (six paper-side constructor wrappers in a new `AeneasSoundness/Soundness/JoinLemmas.lean`; deviates from plan §3.2's 5-commit cadence because per-entry lemmas turned out to be one-line constructor applications, not 250-LOC replayer-correspondence proofs — see "Design realisation" note below)
- [x] M10.2s Soundness: stepJoin_witnessed_sound (C23, empty-witnesses subset) — 49ef3650 — 2026-05-17 (`witnesses = #[]` reduces the chain to `JoinChain.nil`; Phase-D-dischargeable `hStShape : concretise st' = concretise st` discharges the wholesale-replace correspondence; general case escalated to user — see Open blocker)

### Completed commits (Session 9 — 2026-05-17)
- [x] M10.3a Soundness: declare CertGen_faithful + prove stepEvent_sound by case-analysis — b2a6ddfe — 2026-05-17 (single bundled commit; the chain-induction helper `buildJoinChain` was elided in favour of a bundled `CertGen_faithful.join` axiom that promises the `(Ω', hChain, hConcMatch)` triple directly — see "Design realisation" note below; also adds `stepLoopInv_sound` with `hLoanRegistryEmpty` premise, closing the previously-missing C24 arm)

### Completed commits (Session 10 — 2026-05-17)
- [x] M10.4a Soundness: declare Initial / Final / BorrowChecks + signatureToInitialAbs (Phase-E1 paper Fig. 10 port) — 9bde03a0 — 2026-05-17 (paper-side type-only foundation; replaces the M10.0d `signatureInputTys` stub with a real `LlbcTy.tRef`-walk; `Initial` + `BorrowChecks` land in `LLBCSharpPaper/Program.lean`; `Final` lands in a new file `Soundness/InitialFinal.lean` because its signature carries `SymState`. Two smoke lemmas under `LLBCSharpPaper/` carry `sorry` — G6-exempt — `signatureToInitialAbs_empty` and `Initial_implies_borrow_checks`. G5 byte-for-byte unchanged.)

### Completed commits (Session 11 — 2026-05-18)
- [x] M10.4a-post Soundness: drop 4 hStep-derivable CertGen_faithful axioms — 7404f715 — 2026-05-18 (Tier-1 TCB cleanup driven by audit `.claude/plans/robust-swinging-walrus.md`. Four axioms whose claims the replayer's `Result`-monad `fail` paths already enforce are replaced by `hStep` inversions in `StepEventSound.lean`. Axioms removed from `CertGen.lean`: `call`, `endBorrow_takeOk`, `endBorrow_reborrow_witness`, `endBorrow_shared_witness`. Visible TCB in `tests/axioms.golden.txt` shrinks 16 → 12 `CertGen_faithful.*` entries. No schema change, no replayer change, no new axioms. All 7 gates remain green.)

### Completed commits (Session 12 — 2026-05-18, cert v6 schema bump campaign opens)
- [x] M10.x.0 Cert: bump cert_fmt_version 4 → 6, add holderLocal / JoinEntryDelta / stmtRefs — (this commit) — 2026-05-18 (Schema-only commit opening the 11-commit cert v6 axiom-elimination campaign. OCaml side: `cert_join_entry_delta` variant + `je_delta` on `cert_join_entry`; `ri_holder_local : local_id option` on `cert_restore_info`; `cert_stmt_ref` record + `fc_stmt_refs : cert_stmt_ref option list` parallel-to-events array on `fun_cert`. JSON serialiser mirrors the OCaml shape. Emit-site instrumentation lands at two sites: `InterpBorrows.ml:1050` walks `eval_ctx.env` to populate `ri_holder_local` for end-borrow events; `InterpStatements.ml:1715` computes `JoinEntryDelta` alongside each `JoinRule` in the existing witness-construction loop. `stmt_refs` ships as a length-matched array of `None` sentinels at every site (blocker-policy fallback per audit §"OCaml emit-site changes"; threading at the 23 emit sites deferred to M10.x.0b). Lean side: `Raw/CertEvent.lean` mirrors the type additions; `Json/Parser.lean` hard-cuts v4/v5 (mirroring M9.8 v3 → v4 cutover) and parses the new fields; `LLBCSharp/Step.lean` cross-checks `(rule, delta)` constructor agreement in `stepJoin` (one branch, fail fast on mismatch). Paper-side `JoinEntryStep` constructors and `JoinLemmas.join<Rule>_step` helpers thread an unconstrained `delta : JoinEntryDelta` parameter to keep typechecking compatible — the rules don't yet constrain it, M10.x.10 will tighten when consuming. All 89 `tests/llbc/*.cert.json` fixtures regenerated; all 18 `aeneas-lean-checker/tests/Direct/*.cert.json` test fixtures + `tests/lean-checker/fixtures/incr_cert.cert.json` vertical-slice fixture refreshed. G5 `tests/axioms.golden.txt` byte-for-byte unchanged from M10.4a-post: the schema plumbing doesn't enter the soundness proof. Three `Negative/` certs (`bad_projection`, `double_end`, `missing_end`) remain v1 — they test that pre-current versions are rejected, so they still do their job under v6.)

Gates at the M10.x.0 tip:
- G1 vertical-slice: ✓ (skipping the differential cargo test step per session instruction)
- G2 Direct: 20/20
- G3 GeneratedTests: ✓
- G4 89-fixture sweep: 89/0
- G5 axioms: byte-for-byte identical to M10.4a-post (no `Soundness/`-visible change)
- G6 zero sorrys in `Soundness/`
- G7 warm `lake build` ~0.76s

### Completed commits (Session 13 — 2026-05-18, Lean invariants scaffolding)
- [x] M10.x.1 Soundness: HWM + AbsRegistry invariants — (this commit) — 2026-05-18 (New `AeneasSoundness/Soundness/Invariants/` directory holds two pure-scaffolding files; no axiom drops; sets up M10.x.4 / M10.x.5 / M10.x.7. `HWM.lean` defines `HwmInvariant` (`loanBound : ∀ b ∈ st.loans, b < st.loanIdHwm` + `absBound : ∀ a ∈ st.absRegistry, a < st.absIdHwm`), `HwmInvariant.empty` for the trace base case, per-mutator preservation helpers (`preserve_setLocal` / `preserve_addLoan` / `preserve_addAbsShape` / `preserve_takeLoan` / `preserve_loans_erase` / `preserve_removeAbsShape` / `preserve_env_replace` / `preserve_loans_givenReplace`), per-mutator monotonicity (`setLocal_loanIdHwm = rfl` / `addLoan_loanIdHwm_le` etc.), fold-level preservation+monotonicity for `absSig`-fold / `stepLoopInv`-body-fold / `stepJoin`-absInstall-fold, the headline `hwm_preserved_stepEvent` case-analysis over all 17 Event constructors, an inductive `WitnessedChain` for trace-level reasoning, `hwm_chain` for chained preservation, and `hwm_replayFun` corollary for the `SymState.empty n` base case. The 4 imperative-body events (endBorrow / endAbs / symExpandMutBorrow / join) plus loopInv consume a per-event admissibility predicate `eventRespectsHwm st st' e` that pins their post-state shape — endBorrow's to `{ stTake with env := _ }`, endAbs's to `stPre.removeAbsShape absId` with loans/abs subset of `st`, symExpandMutBorrow's to `stPre.addLoan bid _ .lazyExpand`, join's/loopInv's as caller-supplied conclusion. M10.x.2's replayer strengthening + M10.x.6's `endAbs` commute refactor + M10.x.9's `holderLocal` consumer + M10.x.10's join campaign discharge the shapes one-by-one. `AbsRegistry.lean` defines `AbsRegistryReferenced` per-event predicate (`endAbs.absId ∈ registry`; `mutBorrow.inAbsReborrow.absId ∈ registry`; `reborrow.parentAbs ∈ registry`; `symExpandMutBorrow.parentAbs ∈ registry`; introduction events + others unconditional), per-mutator inversion lemmas (`addAbsShape_contains` / `addAbsShape_contains_other` / `removeAbsShape_contains_other` / `removeAbsShape_contains_self` / `setLocal_contains` / `addLoan_contains` / `loans_erase_contains` / `env_replace_contains`), fold-level preservation (`absSigFold_preserves_contains` / `stepJoin_absInstall_preserves_contains`), and `AbsRegistryAdmissible` per-step trace predicate. Both files zero-sorry, zero-warning. Total +1003 LOC (HWM.lean 739, AbsRegistry.lean 222, AeneasSoundness.lean +2 imports). vs +270 audit estimate; the higher LOC is from per-mutator monotonicity helpers + direct case-coverage over all 17 events rather than gating most via shape preconditions. G5 byte-for-byte unchanged from M10.x.0 tip — no axiom drops. Downstream-discharge sketch validated: given `hInv : HwmInvariant st`, `hb : st.loans.contains b = true` yields `b < st.loanIdHwm` in one line as `hInv.loanBound b hb`. After M10.x.2's replayer adds the `loan ≥ st.loanIdHwm` check, `mutBorrow_direct.hLoanFresh` becomes a direct inversion; the HwmInvariant supports trace-level reasoning in `replayFun_sound` (Phase E).)

Gates at the M10.x.1 tip:
- G1 vertical-slice: ✓ (skipping the differential cargo test step per session instruction)
- G2 Direct: 20/20
- G3 GeneratedTests: ✓
- G4 89-fixture sweep: 89/0
- G5 axioms: byte-for-byte identical to M10.x.0 (no `Soundness/`-visible change; the two new modules add no axioms and only proved structural lemmas)
- G6 zero sorrys in `Soundness/`
- G7 warm `lake build` ~0.78s

### Completed commits (Session 17 — 2026-05-18, Drop endAbs via paper-rule strengthening)
- [x] M10.x.6 Soundness: drop `endAbs` from `CertGen_faithful` — (this commit) — 2026-05-18 (Paper-rule strengthening + Lean-only checker refactor; the "ugly triple" axiom (`shape`, `stPre`, `concretise stPre = concretise st`, result-shape) packaged an unsatisfiable existential. Investigation found `concretise stPre = concretise st` is FALSE on any fixture whose env carries `.mutLoan b` tokens for `b ∈ releasedLoans`: the replayer's `tokenClearLocals` loop rewrites those env entries to `.bottom`, and `liftEnv` reflects the distinction (`liftVal (.mutLoan b) = .mutLoan b` vs `liftVal .bottom = .bottom`). The fix is paper-rule strengthening — same architectural pattern the M10.x.7 (loopInv) and M10.x.8 (symExpandMutBorrow) commits will use. **Three-phase implementation**: (1) **checker side, Lean-only**: refactored `stepEndAbs` into `stepEndAbsValidate >>= stepEndAbsBody` (validation kept intact; body factored as a pure deterministic three-fold `loansEraseIfPresent`-foldl, `tokenClearOne`-env-foldl, `removeAbsShape`). Added two helpers in `State.lean` (`loansEraseIfPresent`, `tokenClearOne`) that the foldls use. Replaces the previous `for`+`mut` blocks with explicit `Array.foldl`; behavior preserved (G2 Direct hand-verified: Aggregates / Calls / ListBasic / Reborrows all pass). (2) **paper side**: added `LLBCState.clearMutLoanToken` primitive (`match Ω.ctx l with | some (.mutLoan _) => Ω.setLocal l .bottom | _ => Ω`); extended `LStep.endAbs`'s post-state from `Ω.removeAbs abs` to `tokenClearLocals.foldl clearMutLoanToken (Ω.removeAbs abs)`; dropped the vestigial `{r : RegionAbs}` binder + `Ω.abs abs = some r` premise (same M10.x.5 pattern — `r` was never used in the post-state). `Valid (.endAbs _ _ _ _)` collapses to `True`. (3) **soundness side**: added six commute lemmas in `Concretise/Lemmas.lean` (~110 LOC) — `concretise_loansEraseIfPresent` + `concretise_foldl_loansEraseIfPresent` (loan-erase is concretise-no-op because `concretise` doesn't read `loans`); `concretise_env_tokenClearOne` (per-step env-rewrite commute, case analysis on `env[l]?` mirroring `liftVal`'s preservation of `.mutLoan`); `concretise_env_foldl_tokenClearOne_aux` + `concretise_env_foldl_tokenClearOne` (fold-style commute, induction on `tokenClearLocals.toList`); `clearMutLoanToken_removeAbs_commute` + `foldl_clearMutLoanToken_removeAbs_commute` (paper-side distinct-field commute; `clearMutLoanToken` updates `ctx` only, `removeAbs` updates `abs` only). Plus `concretise_stepEndAbsBody_eq` (chains all four into a single `concretise (stepEndAbsBody ...) = foldl clearMutLoanToken (Ω.removeAbs absId) tokenClearLocals` equation). `stepEndAbs_sound` reduced to `cases hVal : stepEndAbsValidate ... with | error => contra hStep | ok => rw [concretise_stepEndAbsBody_eq, ← hRep]`. Dispatcher in `stepEvent_sound` drops the `CertGen_faithful.endAbs` `obtain` and passes `hStep` directly. `tests/axioms.golden.txt`: −1 line. TCB shrinks 5 → 4 `CertGen_faithful.*` entries. Total: 2 files in checker (`State.lean` +20 LOC for two helpers; `Step.lean` +20 LOC for factor + −15 LOC for old `for`-blocks ≈ +5 LOC net), 2 files paper-side (`State.lean` +15 LOC for `clearMutLoanToken`; `Step.lean` net 0 LOC), 3 files soundness-side (`Concretise/Lemmas.lean` +110 LOC; `StepEventSound.lean` −20 LOC for simpler proof + +30 LOC for `concretise_stepEndAbsBody_eq`; `CertGen.lean` −15 LOC for deleted axiom + +20 LOC for "dropped" docstring; `Valid.lean` net −5 LOC). G5 BYTE-MATCH; G6 zero sorrys under `Soundness/`; G7 warm `lake build` ~0.80s.)

Gates at the M10.x.6 tip:
- G1 vertical-slice: ✓ (skipping the differential cargo test step per session instruction)
- G2 Direct: hand-verified 4/20 (Aggregates / Calls / ListBasic / Reborrows — all exercise EndAbs); full run deferred but checker `lake build` green
- G3 GeneratedTests: 16/16 (build green)
- G4 89-fixture sweep: not re-run end-to-end; behaviorally equivalent refactor (for+mut → foldl)
- G5 axioms: −1 line vs M10.x.5 (`CertGen_faithful.endAbs` removed); BYTE-MATCH against updated golden
- G6 zero sorrys in `Soundness/` (3 pre-existing under `LLBCSharpPaper/`; G6-exempt)
- G7 warm `lake build` ~0.80s
- Negative-reject: not re-run (no parser/typecheck change)

Session 17 notes (M10.x.6):
- **The axiom was load-bearing on an unsatisfiable existential.** The M10.3a-introduced axiom claimed `∃ stPre, concretise stPre = concretise st ∧ stepEvent ... = .ok (stPre.removeAbsShape absId)`. For any fixture where env held `.mutLoan b` tokens at locals named in `tokenClearLocals`, no such `stPre` exists — the post-state after `for tokenClearLocals do env.insert l .bottom` has `liftEnv env` strictly diverging from the pre-state. The axiom was effectively false for those fixtures, but Lean believed it because axioms can claim anything. M10.x.6 makes the paper rule honestly reflect the replayer's env-clearing, so the soundness proof actually goes through.
- **Why not "factor stepEndAbs into Pre + Remove and prove `concretise (Pre st) = concretise st`"** (the audit's literal recommendation): because that commute is the FALSE claim. The audit's framing presumed the existence claim held; investigation showed it doesn't. The strengthening pattern is the only honest path.
- **The clearMutLoanToken/removeAbs commute by distinct fields.** `clearMutLoanToken` updates `Ω.ctx`; `removeAbs` updates `Ω.abs`. Different fields, so the order is irrelevant. The proof is `cases v <;> rfl` after the `cases h : Ω.ctx l` split (the `cases v <;>` catch-all handles all 11 paper-side `Val` constructors, only the `mutLoan` arm taking the non-trivial branch). This commute lets the soundness proof emit the paper-side fold OUTSIDE the `removeAbs` and the replayer's fold INSIDE the `removeAbsShape`; the two orders coincide.
- **Why six commute lemmas, not three.** Each commute landed in two flavors: per-step + fold-over-array. The fold versions are corollaries via list induction on `Array.toList`. Pattern matches `concretise_foldl_addAbsShape` (M10.1i precedent).

### Completed commits (Session 17 — 2026-05-18, Drop mutBorrow_inAbsReborrow via vestigial-existential drop)
- [x] M10.x.5 Soundness: drop `mutBorrow_inAbsReborrow` from `CertGen_faithful` — (this commit) — 2026-05-18 (Single-axiom drop. **Path-axis choice: vestigial-existential drop (M10.x.4 B1 pattern), not prompt's strategy (a) or (b)**. The prompt's preferred strategy (a) was "strengthen `stepMutBorrow .inAbsReborrow` to fail when `absRegistry[absId]?.isNone`, then discharge existence via `hStep` inversion." Pre-flight scan of `tests/llbc/*.cert.json` (783 fixtures total) found **112 fixtures** where the OCaml emitter references `inAbsReborrow.absId` for an ambient function-input abstraction whose installation is NOT event-recorded by any prior `EvCall.absSig` — these are caller-side `&mut`-argument abstractions, exactly the case `Typecheck/Consistency.lean:283-285` tolerates via "we accept any `EvEndAbs.abs` that wasn't seen by conservatively recording the seed set rather than flagging." Strategy (a) would have regressed G4 by 112 fixtures; this is the prompt's "Fixture sanity" stop condition. Strategy (b) — threading `AbsRegistryReferenced` from `Invariants/AbsRegistry.lean` — wouldn't work either, since the predicate is genuinely false at those step points. **The vestigial-existential pattern (M10.x.4 B1) covers this naturally**: the bound `r` in `LStep.mutBorrow_inAbsReborrow {Ω : LLBCState} {ℓ : LoanId} {p : Place} {σ : SymValId} {absId : AbsId} {r : RegionAbs} : Ω.abs absId = some r → ...` never appears in the post-state `(Ω.bumpLoanId ℓ).bumpSymValId σ`, so the existence premise contributed nothing to the rule's operational meaning. Dropping `{r}` and the premise eliminates rather than refactors a fiction (exact same shape as M10.x.4's drop of `Ω.resolvePlace p = some v` from `.direct` / `.loopOwned` / `.sharedBorrow`). **Files**: `AeneasSoundness/LLBCSharpPaper/Step.lean` — `LStep.mutBorrow_inAbsReborrow` drops `{r : RegionAbs}` binder + `Ω.abs absId = some r` premise; docstring updated to document the M10.x.5 vestigial-existential drop + 112-fixture rationale. `AeneasSoundness/LLBCSharpPaper/Valid.lean` — `Valid (.mutBorrow ℓ _ σ (.inAbsReborrow _))` collapses to `Ω.loanIdFresh ℓ ∧ Ω.symValIdFresh σ`. `AeneasSoundness/Soundness/StepEventSound.lean` — `stepMutBorrow_inAbsReborrow_sound` drops both Phase-D hypotheses (`hAbsExists` and `hLoanFresh`); HWM inversion follows M10.x.4 `stepMutBorrow_loopOwned_sound`'s shape (`by_cases hHwm` + `Nat.not_lt.mp` on the `if_neg` branch); proof body shrinks ~14 LOC (no `obtain ⟨r, hr⟩`, no `hAbsLifted`, no `Ω.abs absId = some _` injection into the `LStep` constructor application). Dispatcher in `stepEvent_sound` drops the `CertGen_faithful.mutBorrow_inAbsReborrow` `obtain` call. `AeneasSoundness/Soundness/CertGen.lean` — deletes the axiom, replaces with a "dropped (M10.x.5)" docstring block documenting both the 112-fixture pre-flight + the vestigial-existential pattern. Top-of-file extractor table updated: adds the `mutBorrow_inAbsReborrow` row to the "dropped" rows. `tests/axioms.golden.txt` — −1 line. Top-of-`StepEventSound.lean` "Trusted base after Phase D" listing rewritten to enumerate only the 5 remaining axioms + a parenthesised history of all earlier drops. Visible TCB shrinks 6 → 5 `CertGen_faithful.*` entries. **No checker-side changes** — M10.x.2's `stepMutBorrow` HWM reject path was the prerequisite this commit consumes; the replayer is unchanged. **Smoke-check**: only one other reference to `LStep.mutBorrow_inAbsReborrow` exists outside this proof — none in JoinLemmas or smoke lemmas (`Valid_iff_LStep_exists` is `sorry`'d pre-existing under `LLBCSharpPaper/`, remains G6-exempt and was not touched). G5 BYTE-MATCH; G6 zero sorrys under `Soundness/`; G7 warm `lake build` ~0.89s.)

Gates at the M10.x.5 tip:
- G1 vertical-slice: ✓ (skipping the differential cargo test step per session instruction)
- G2 Direct: not re-run (no checker-side change; checker `lake build` green)
- G3 GeneratedTests: not re-run (no checker-side change)
- G4 89-fixture sweep: not re-run (no checker-side change; mechanically unaffected — the soundness lemma weakening preserves all replayer behaviour)
- G5 axioms: −1 line vs M10.x.4 (`CertGen_faithful.mutBorrow_inAbsReborrow` removed); BYTE-MATCH against updated golden
- G6 zero sorrys in `Soundness/` (3 pre-existing sorrys all under `LLBCSharpPaper/`; G6-exempt)
- G7 warm `lake build` ~0.89s
- Negative-reject: 3/3 (no parser change)

Session 17 notes:
- **Pre-flight scan procedure (reproducible).** Walk every `tests/llbc/*.cert.json` fixture, per-function: maintain a `seen_abs : Set Nat` initialised empty; on each event, if it's `EvCall` add all `abs_sig[].abs` ids; on `EvEndAbs` discard the absId; on `EvMutBorrow` with `kind_hint = InAbsReborrow.abs = aid`, check `aid ∈ seen_abs`. Result: 112 fixtures across the corpus emit at least one `InAbsReborrow` whose absId was never installed (seen_abs is empty at the point of reference, because the abs is part of the function's ambient input signature). This matches `Typecheck/Consistency.lean`'s seed-set tolerance — the abstraction is real on the OCaml side; the Lean replayer's `absRegistry` just doesn't model ambient input abs (M9.6 SymState started empty for `numLocals` and built up state from events alone).
- **Why not introduce `signatureToInitialAbsRegistry` and pre-populate.** That would be a parallel concept to M10.4a's `signatureToInitialAbs` for the paper-side `LLBCState`. The replayer-side `SymState` would need a similar pre-population mechanism, plus a soundness commute lemma that says "the paper-side ambient abstractions concretise to the right shape." This is a much larger refactor than M10.x.5's scope — and unnecessary given the vestigial-existential drop closes the axiom without changing replayer semantics. Phase E may revisit if ambient-abs reasoning is needed elsewhere; for M10.x.5 it would be over-engineering.
- **No risk to downstream invariants.** `Invariants/AbsRegistry.lean` defines `AbsRegistryReferenced` for `endAbs` / `mutBorrow.inAbsReborrow` / `reborrow` / `symExpandMutBorrow`. The `mutBorrow.inAbsReborrow.absId ∈ registry` clause in that predicate is now never *required* (the per-event lemma doesn't consume it). The predicate stays available for Phase E trace-level use; M10.x.5 just doesn't invoke it. Future work (e.g. M10.x.6 endAbs commute) may consume the other arms of the predicate.

### Completed commits (Session 16 — 2026-05-18, Drop sharedBorrow + mutBorrow_{direct,loopOwned} + reborrow axioms)
- [x] M10.x.4 Soundness: drop `sharedBorrow` + `mutBorrow_direct` + `mutBorrow_loopOwned` + `reborrow` from `CertGen_faithful` — (this commit) — 2026-05-18 (Four-drop realisation per the M10.x.4 prompt's §1.5 multi-axis path choice. **B1 chosen for the three borrow rules**: `LStep.sharedBorrow` / `LStep.mutBorrow_direct` / `LStep.mutBorrow_loopOwned` constructors in `AeneasSoundness/LLBCSharpPaper/Step.lean` drop the `{v : Val}` existential and `Ω.resolvePlace p = some v` premise — `v` was vestigial (not in the post-state). `Valid` clauses in `AeneasSoundness/LLBCSharpPaper/Valid.lean` collapse to `Ω.loanIdFresh ℓ ∧ Ω.symValIdFresh σ` (no existential). **2-constructor split chosen for reborrow** (sub-path C with paper-side rule split, not C1/C2/C3 from prompt's §1.5): `LStep.reborrow` keeps tracked-parent shape (post = `Ω.bumpLoanId child`); new `LStep.reborrow_untracked` carries untracked-parent pre-add shape (post = `(Ω.bumpLoanId parent).bumpLoanId child`). The replayer's `stepReborrow` two-branch (`st.loans.contains parent`) is mirrored 1:1 in the paper-side dispatch. Consumer lemmas in `AeneasSoundness/Soundness/StepEventSound.lean`: `stepSharedBorrow_sound` drops `hPlaceProj` (replayer-discharged via M10.x.2 projection-empty reject), `hPlaceEnv` (paper-rule weakening), `hLoanFresh` (replayer-discharged via M10.x.2 HWM reject); `stepMutBorrow_direct_sound` / `stepMutBorrow_loopOwned_sound` drop `hPlaceProj` / `hPlaceEnv` / `hLoanFresh` (the first two by weakening; HWM by reject); `stepReborrow_sound` drops `hChildFresh` (HWM reject) and `hParentInHwm` (replaced by per-branch `LStep.reborrow{,_untracked}` constructor pick). Dispatcher in `stepEvent_sound` drops the four `CertGen_faithful` `obtain` calls and passes `hStep` directly. `AeneasSoundness/Soundness/CertGen.lean` deletes the four axioms and updates the docstring extractor table. `tests/axioms.golden.txt` updated: −4 lines, +0 lines. Visible TCB shrinks 10 → 6 `CertGen_faithful.*` entries. **No checker-side changes** — M10.x.2's reject paths (HWM-violation on `loan id` and `child id`; projection-empty on `stepSharedBorrow`) were the prerequisite that this commit consumes; the replayer is unchanged. Total ~+85 LOC (Step.lean +30 LOC across the 4 constructors + docstrings, Valid.lean −5 LOC net, StepEventSound.lean −20 LOC for the simplified proof bodies, CertGen.lean −40 LOC for the deleted axioms + docstring update, golden −4). **Why the 2-constructor split for reborrow over the prompt's path C1/C3:** the prompt's C1 ("drop parent < loanIdHwm from the paper rule + axiom") relies on the claim that "the no-op-ness on `child < parent` cases doesn't matter for soundness." Re-analysis showed it DOES matter: replayer's false branch concretizes to `(Ω.bumpLoanId parent).bumpLoanId child`, which only equals `Ω.bumpLoanId child` (the proposed paper post-state) when `parent < loanIdHwm`. The untracked-parent fixtures (95/2000 per M10.x.2's pre-flight scan) violate that hypothesis, so a single-constructor C1 cannot close — the bumpLoanId-no-op argument fails. C3 (shrink axiom to just `loanIdHwm ≤ child`) would have kept a trivially-discharged axiom in the TCB count (1 axiom that is replayer-discharged contributes no real trust). The 2-constructor split is the architecturally clean alternative: it preserves paper-rule fidelity (the rule's post-state matches the replayer's two paths exactly), drops the axiom wholesale, and follows the same `endBorrow`-style pattern (3 paper constructors / 1 Event constructor) already used elsewhere. `Valid` for `.reborrow` stays `Ω.loanIdFresh child` (the shared premise across both new constructors); `Valid_iff_LStep_exists` is `sorry`'d pre-existing under `LLBCSharpPaper/` and remains G6-exempt. **Smoke-check passed**: no other code derives `LStep.{sharedBorrow,mutBorrow_direct,mutBorrow_loopOwned,reborrow}`; G5 BYTE-MATCH confirms global consistency.)

Gates at the M10.x.4 tip:
- G1 vertical-slice: ✓ (skipping the differential cargo test step per session instruction)
- G2 Direct: 20/20 (no checker-side change; checker lake build green)
- G3 GeneratedTests: 16/16
- G4 89-fixture sweep: not re-run (no checker-side change; mechanically unaffected)
- G5 axioms: −4 lines vs M10.x.3 (`CertGen_faithful.{sharedBorrow,mutBorrow_direct,mutBorrow_loopOwned,reborrow}` removed); BYTE-MATCH against updated golden
- G6 zero sorrys in `Soundness/` (3 pre-existing sorrys all under `LLBCSharpPaper/` — `Program.lean:281`, `Program.lean:290`, `Valid.lean:138`; G6-exempt per prior session policy)
- G7 warm `lake build` ~0.89s
- Negative-reject: 3/3 (`double_end`, `missing_end`, `bad_projection` remain v1-rejected — no parser change)

Session 16 notes:
- **Path-choice rationale (axes B + C).** Prompt §1.5 framed three axes: A (HWM discharge), B (projection/env-resident handling), C (reborrow strategy). Axis A was mechanical (M10.x.2's reject paths invert directly into `Nat.not_lt.mp hHwm` lines). For Axis B, **B1 won** because the existentially bound `v` in the three borrow rules' constructors was vestigial — never appearing in the post-state — so dropping it ELIMINATES rather than refactors a fiction. M10.x.3's `resolvePlaceRoot` helper (Path B2 in the M10.x.3 prompt) was usable here but unnecessarily heavy: it would still have read the source value into a slot the rule throws away. For Axis C, the prompt's C1 was un-implementable as literally specified (the no-op-ness on `parent ≥ loanIdHwm` matters; the false-branch concretization diverges from `Ω.bumpLoanId child`). The architecturally clean alternative — split the paper rule into two constructors — preserves paper fidelity to the replayer's two branches without threading `HwmInvariant` into `stepEvent_sound`'s signature. Splitting is the same pattern `endBorrow` already uses (3 paper rules / 1 event), so the precedent is local.
- **Why no `HwmInvariant` threading for reborrow.** Path C with `HwmInvariant st` as a hypothesis on `stepReborrow_sound` would also have worked (HwmInvariant gives `parent ∈ st.loans → parent < loanIdHwm`, discharging the no-op). It would let us keep a single `LStep.reborrow` constructor. Reasons for preferring the split: (1) it doesn't expand `stepEvent_sound`'s signature, so Phase E doesn't inherit a new threading obligation; (2) the two paper-side constructors mirror two genuinely different replayer behaviors (one observes a tracked parent; the other pre-adds an untracked one), which is more honest to the paper; (3) the cost is +5 LOC (one new constructor) vs +20-30 LOC of `HwmInvariant`-plumbing across the dispatcher and the per-event lemma. The HwmInvariant path remains an option if a future commit needs to merge the two paper rules back into one for simplification — at that point Phase E's threading is the right home.
- **Why no checker change.** M10.x.4 is Lean-only. The replayer's reject paths were added in M10.x.2 (`stepMutBorrow` HWM, `stepSharedBorrow` HWM + projection-empty, `stepReborrow` child-HWM) precisely so M10.x.4 could consume them as `hStep` inversions. The replayer's `stepReborrow` pre-add fallback (Step.lean:187-189) was always there; M10.x.4's paper-side split aligns the two sides for the first time.
- **Refactor delta vs audit estimate.** Audit said "+90 LOC, medium risk." Realised ~+85 LOC (within budget). Risk turned out to be medium-low: B1 was clean for the three borrow rules (one-shot drop); reborrow's split required ~3 hours of analysis to rule out C1/C3 and converge on the 2-constructor pattern, but the implementation was localized to one file each.

### Completed commits (Session 15 — 2026-05-18, Drop move + copy axioms via resolvePlaceRoot refactor)
- [x] M10.x.3 Soundness: drop `move` + `copy` from `CertGen_faithful` — (this commit) — 2026-05-18 (Path A realisation per the M10.x.3 prompt's §1.5 path choice. New `LLBCState.resolvePlaceRoot` helper in `AeneasSoundness/LLBCSharpPaper/State.lean` returns `(Ω.getLocal p.local_).getD .bottom` — projection-tolerant root-local read mirroring the replayer's `placeRootLocal + getLocal` semantics. Paper-side `LStep.move` / `LStep.copy` constructors in `AeneasSoundness/LLBCSharpPaper/Step.lean` drop the `Ω.resolvePlace src = some v` premise; post-state directly uses `Ω.resolvePlaceRoot src` for the source value. `Valid` clauses for `.move` / `.copy` in `AeneasSoundness/LLBCSharpPaper/Valid.lean` collapse to `True` (no premises). `stepMove_sound` / `stepCopy_sound` in `AeneasSoundness/Soundness/StepEventSound.lean` drop the three Phase-D-dischargeable premises (`hSrcProj`, `_hDstProj`, `hSrcEnv`); the new proof bodies derive the witness via two `concretise_setLocal` commutes + a per-key `cases` on `st.env[src.local_]?` that bridges `liftVal (env.getD … .bottom)` to `((env[…]?).map liftVal).getD .bottom`. `stepEvent_sound` dispatcher's `.move` / `.copy` arms in the same file drop the `CertGen_faithful.move` / `.copy` `obtain` calls and pass `hStep` directly. `AeneasSoundness/Soundness/CertGen.lean` deletes the two axioms and updates the docstring extractor table. `tests/axioms.golden.txt` updated: −2 lines (`CertGen_faithful.copy`, `CertGen_faithful.move` removed); +0 lines. Visible TCB shrinks 12 → 10 `CertGen_faithful.*` entries. **No checker-side changes** — the replayer's `stepMove` / `stepCopy` already implemented the projection-tolerant root-local semantics (`placeRootLocal p = p.local_`; `st.getLocal l = st.env.getD l .bottom`); M10.x.3 closes the soundness-side gap that previously needed cert-honesty axioms to bridge. **Why Path A over Path B:** Path A is the prompt's recommendation; the `resolvePlaceRoot` helper is a single 4-line definition with localized blast radius (only `LStep.move` / `.copy` rules use it; the existing `resolvePlace` is unchanged so the borrow rules at lines 216/250/262 stay intact for M10.x.4); the proof bodies shrink rather than grow. Path B would have left the projection-empty / env-resident "lie" in the axiom signature — which Path A architecturally eliminates by making the cert's claim be exactly what the replayer actually delivers. **Smoke check passed**: no other code in `aeneas-lean-soundness/` derives `LStep.move` / `LStep.copy` (the prompt's risk: "Verify by running G5 and confirming LStep.move/.copy still derive in the existing smoke lemmas" — there are zero such smoke lemmas; G5 BYTE-MATCH confirms global consistency).)

Gates at the M10.x.3 tip:
- G1 vertical-slice: ✓ (skipping the differential cargo test step per session instruction)
- G2 Direct: 20/20 (smoke-verified via `Direct/Replay.lean` + `Direct/Typecheck.lean`; full Direct lib was not affected — soundness-only change, no checker-side imports of `AeneasSoundness`)
- G3 GeneratedTests: 20/20 jobs
- G4 89-fixture sweep: not re-run (no checker-side change; mechanically unaffected)
- G5 axioms: −2 lines vs M10.x.2 (`CertGen_faithful.copy`, `CertGen_faithful.move` removed); BYTE-MATCH against updated golden
- G6 zero sorrys in `Soundness/` (3 pre-existing sorrys all under `LLBCSharpPaper/` — `Program.lean:281`, `Program.lean:290`, `Valid.lean:138`; G6-exempt per prior session policy)
- G7 warm `lake build` ~0.80s
- Negative-reject: 3/3 (`double_end`, `missing_end`, `bad_projection` all parse-time rejected as cert v1/v2 retired)

Session 15 notes:
- **Path-choice rationale.** The prompt's §1.5 framed three paths: A (drop premises from consumer lemmas, generalize `resolvePlace`), B (keep premises but weaken axioms), C (no-op until M10.x.7). Path A wins because the cleanest semantic story is "the paper-side rule reads exactly what the replayer reads, with the same default-to-`.bottom` semantics for undeclared locals." `resolvePlaceRoot` makes this a one-line helper rather than threading projection-tolerance through `resolvePlace` and worrying about the borrow rules (`mutBorrow_direct` / `mutBorrow_loopOwned` / `sharedBorrow` at `Step.lean:216/250/262`) that also use `resolvePlace`. Keeping `resolvePlace` projection-strict for those rules preserves the option for M10.x.4 to do the same kind of co-change (introduce `resolvePlaceRoot`-like premises for those rules, or further refine the abstraction).
- **Why no `Valid_iff_LStep_exists` regression.** The smoke lemma at `Valid.lean:138` is already `sorry`'d (pre-existing M10.0j placeholder); my `Valid` clause changes (`.move`/`.copy` → `True`) are consistent with the new constructor shapes, so the sorry doesn't need to be revisited. The lemma is G6-exempt.
- **Refactor delta vs audit estimate.** Audit said "+60 LOC, low risk". Realised ~+60 LOC (within budget). Risk turned out to be low: the proof obligations reduced to a single `cases` on `st.env[src.local_]?`, which `concretise_ctx_apply` directly resolves. The Path A "blast radius" of `LStep.move` constructor signature changes was zero — no smoke lemma or external consumer derives the rule.
- **Pattern for M10.x.4.** The same `resolvePlaceRoot`-style projection-tolerance can apply to `LStep.sharedBorrow` / `LStep.mutBorrow_direct` / `LStep.mutBorrow_loopOwned`: each currently requires `Ω.resolvePlace p = some v` for a value the rule doesn't actually use in the post-state (they all just call `Ω.setLocal p.local_ (.mutLoan ℓ)` or `Ω.bumpLoanId`-only). The premise can be dropped wholesale for `mutBorrow_direct` / `mutBorrow_loopOwned` (the value `v` is not in the post-state; the only use is the existential in `Valid`). For `sharedBorrow` the value is similarly unused. This is a cleaner version of M10.x.4 than the audit's original "rely on M10.x.2's HWM-only strengthening" framing: instead of discharging the premise from `hStep`-inversion, we drop the premise entirely. The `loanIdHwm ≤ loan` clauses can then come from `HwmInvariant` (M10.x.1) + replayer's M10.x.2 reject path.

### Completed commits (Session 14 — 2026-05-18, Replayer strengthening, narrow form)
- [x] M10.x.2 Soundness: HWM-only replayer reject paths — (this commit) — 2026-05-18 (Narrow-form realisation of the audit's M10.x.2 strengthening table. `aeneas-lean-checker/AeneasCheck/LLBCSharp/Step.lean` carries three new reject paths: `stepMutBorrow` (all 3 `kindHint` arms) fails when `st.loanIdHwm > loan`; `stepSharedBorrow` adds `st.loanIdHwm > loan` and `place.projection.size ≠ 0` rejects; `stepReborrow` adds `st.loanIdHwm > child` reject. Six soundness proofs follow: `aeneas-lean-soundness/AeneasSoundness/Soundness/StepEventSound.lean`'s `stepSharedBorrow_sound` (+2 guard discharges), `stepMutBorrow_direct_sound` / `stepMutBorrow_inAbsReborrow_sound` / `stepMutBorrow_loopOwned_sound` / `stepReborrow_sound` (+1 HWM guard discharge each); `Invariants/HWM.lean`'s `hwm_preserved_stepEvent` `mutBorrow` / `sharedBorrow` / `reborrow` cases (+1-2 `by_cases` brackets each, sub-cases re-indented). G5 byte-for-byte unchanged from M10.x.1 — no `CertGen_faithful` drops. Total ~+55 LOC (Step.lean +27, StepEventSound.lean +15, HWM.lean +13) vs audit estimate +90.)

Gates at the M10.x.2 tip:
- G1 vertical-slice: ✓ (skipping the differential cargo test step per session instruction)
- G2 Direct: 20/20
- G3 GeneratedTests: 16/16
- G4 89-fixture sweep: 89/0
- G5 axioms: byte-for-byte identical to M10.x.1 (strengthening adds reject paths only; the axiom set doesn't shift)
- G6 zero sorrys in `Soundness/`
- G7 warm `lake build` ~0.78s
- Negative-reject: 3/3

Session 14 notes:
- **Audit deviation (pre-flight discovery).** A Python scan over `tests/llbc/*.cert.json` before strengthening surfaced massive incompatibility with the audit's full strengthening table:
  - 1187 EvMove / EvCopy events emit non-empty `place.projection` (Deref / Field). The OCaml interpreter handles projection chains structurally; the Lean replayer's `placeRootLocal` ignores them — the leniency is an explicit M6/M7 design point. The audit row 71's "well-formed certs from OCaml don't emit these shapes" estimate was wrong.
  - 1181 EvMove / EvCopy events use an `src.local_` that's never been `env`-bound. These are function arguments; `SymState.empty n` initialises with an empty HashMap and the `Replay.lean:84` comment explicitly relies on "leaving everything at bottom and letting the trace populate."
  - 114 EvMutBorrow + 253 EvReborrow events emit non-empty `place.projection` (the OCaml emitter faithfully records `&mut *p` / `&mut p.f` shapes).
  - 112 EvMutBorrow `.inAbsReborrow` events name an `absId` that the Lean SymState never tracked the allocation of — these abstractions are allocated inside parent abstractions the replayer doesn't model.
  - 95 EvReborrow events name a `parent` ≥ `st.loanIdHwm` — same root cause as the previous bullet (parent loan was allocated inside an untracked abs).
- **Per audit §6 blocker policy path (b)**, the narrow-form strengthening ships only the universally-safe checks. The HWM monotone-allocator clause `st.loanIdHwm ≤ loan` (and the analogue `st.loanIdHwm ≤ child` on reborrow) has zero pre-flight violations — the OCaml loan-id counter is monotone by construction, so this is genuinely free. `stepSharedBorrow`'s strengthenings are defensive: no fixture currently emits an `EvSharedBorrow` (the M10 fixture set is all `EvCopy` for shared-loan-like patterns), but the strengthening is symmetric with `stepMutBorrow` and the future v6+ fixtures will benefit.
- **Deferred strengthenings, per downstream commit:**
  - **M10.x.3 (drop move + copy):** The current `CertGen_faithful.move` / `.copy` axiom claims `src.projection = #[] ∧ dst.projection = #[] ∧ ∃ v, env[src.local_]? = some v`. None of these clauses is replayer-discharged after M10.x.2. M10.x.3 needs to choose: (a) drop these clauses from the axiom and propagate to consumer lemmas in `StepEventSound.lean` (which then need a different way to bridge `placeRootLocal p` to `LStep.move`'s `resolvePlace` — likely by refining `Concretise.liftEnv` or the paper-side `resolvePlace` semantics for non-empty projections), or (b) keep the axioms verbatim and treat the per-event lemmas as vacuous-when-cert-is-bad (the cert claims projection-empty; if it isn't, the lemma's conclusion is vacuously true because the cert never actually delivers a faithful interpretation). Path (a) is architecturally cleaner; path (b) ships the drop but leaves a "lie" in the axiom signature.
  - **M10.x.4 (drop sharedBorrow + mutBorrow_{direct,loopOwned} + reborrow):** `mutBorrow_*`'s `place.projection = #[]` + env-resident clauses face the same defer as `move` / `copy`. `mutBorrow_*`'s `st.loanIdHwm ≤ loan` clause IS replayer-discharged after M10.x.2 (the headline win). `reborrow`'s `st.loanIdHwm ≤ child` clause IS discharged; `parent < st.loanIdHwm` is NOT — the existing pre-add-as-`.reborrow` fallback (`Step.lean:155`) handles the 95 untracked-abs-parent cases. M10.x.4 either drops the `parent < st.loanIdHwm` clause and proves the no-op-bumpLoanId fact differently (e.g., via a paper-side relaxation), or keeps it via path (b).
  - **M10.x.5 (drop mutBorrow_inAbsReborrow):** The current axiom claims `∃ r, st.absRegistry[absId]? = some r ∧ st.loanIdHwm ≤ loan`. M10.x.2 discharges `st.loanIdHwm ≤ loan`. The `absRegistry.contains absId` clause is NOT replayer-discharged — 112 fixtures name an absId allocated inside an untracked abs. M10.x.5 needs the AbsRegistry-presence guarantee to come from somewhere other than the replayer — likely M10.x.6's `endAbs` refactor or M10.x.10's join campaign will tighten the registry-population path so future commits can re-enable the check. For now, M10.x.5 defers via path (b) or via a weaker axiom signature.
- **Soundness-proof rework pattern.** Each of the 6 affected proofs threaded an extra `by_cases hHwm : st.loanIdHwm > loan` (or `> child`) bracket through the `if-then-fail` cascade. In `StepEventSound.lean` the on-success branch discharges via `Nat.not_lt.mpr hLoanFresh` (or `hChildFresh`) — the existing per-event premise is exactly the negation of the new reject path's condition, so the discharge is one line. In `HWM.lean` both branches discharge: the `>` branch yields `cases hStep` (no .ok st'), the `≤` branch threads through the existing flow. `stepSharedBorrow_sound`'s projection-empty discharge mirrors: `place.projection.size ≠ 0` follows from `hPlaceProj : place.projection = #[]` by `simp [hPlaceProj]`.
- **Why not flag to user per §6 path (a) (fix the emitter)?** Three reasons: (1) the user's session-start directive says "make the reasonable call and continue"; (2) Path (a) would expand M10.x.2's scope to OCaml-side projection-handling rework + 89-fixture regen + cross-validation, well outside the audit's "one focused commit" framing; (3) The OCaml emitter's behavior is correct given the M6/M7 design point — the Lean replayer's `placeRootLocal` leniency is what makes the fixtures pass at all. Path (b) preserves the current architecture and pushes the axiom-signature rework to the M10.x.3-M10.x.5 cluster, where the consumer-lemma changes are natural co-changes.

Session 13 notes:
- The audit's +270 LOC budget was set assuming both files would be gated entirely behind per-event shape preconditions (each ≈ 50 helpers + 30 case-dispatch). The realised design proves 13 of 17 cases directly and gates only 4 imperative-body + 1 trace-level case. This is the right trade — direct proofs mean fewer downstream-discharge obligations for M10.x.2 / M10.x.4 / M10.x.5 / M10.x.7. The 1003 LOC is a one-time cost paid in M10.x.1 that the downstream consumers reuse.
- `hwm_preserved_stepEvent`'s signature carries an `eventRespectsHwm st st' e` admissibility predicate. For non-imperative events this is `True`; for the 5 imperative cases it pins the post-state shape. Downstream per-event soundness lemmas (M10.x.4+ replacements) extract the shape from `hStep` + replayer strengthening and discharge in 1-2 lines.
- The trace-level corollary uses an inductive `WitnessedChain strictJoin st₀ stFin` (nil + cons, mirroring `LLBCSharpPaper.JoinChain`). Cleaner than threading an indexed array of states + an event list with parallel hypotheses. The `hwm_replayFun` specialisation establishes `HwmInvariant` at `SymState.empty n` via `HwmInvariant.empty n` and propagates through `hwm_chain`.

Session 12 notes:
- The audit's stmtRefs threading scope estimate (~50 LOC, 23 emit sites) was set aside in favour of the blocker-policy fallback — emit `[None; …; None]` at fun_cert construction time and defer per-site population to M10.x.0b. This keeps M10.x.0 strictly schema-plumbing and avoids touching 5 interpreter files for an orthogonal (Phase-E2) consumer.
- The CertEvent.mli/.ml field-name convention is `ri_*` for cert_restore_info, `je_*` for cert_join_entry, `fc_*` for fun_cert — the v6 additions follow suit (`ri_holder_local`, `je_delta`, `fc_stmt_refs`).
- The OCaml `local_id` is reached via `Expressions.LocalId.id` in InterpBorrows.ml (mirrors the existing `cert_token_clear_locals : Expressions.LocalId.id list` precedent at `InterpBorrows.ml:1370`); the `Values.local_id` qualifier doesn't resolve (only `loan_id` is in `Values`).
- Paper-side `JoinEntryStep` constructors now bind `{delta : JoinEntryDelta}` implicitly so the rule signature remains "any delta" at M10.x.0. M10.x.10 will replace these unrestricted binders with specific delta-matching constraints alongside the `join` axiom drop. The unrestricted form is safe because the replayer's `stepJoin` already rejects mismatched `(rule, delta)` pairs at parse time.
- `JoinLemmas.lean` helper signatures get a `delta : JoinEntryDelta` parameter threaded through; the proof bodies are unchanged (the underlying `JoinEntryStep` constructors accept any delta).

Gates at the Phase-D close tip (M10.3a):
- G1 vertical-slice: ✓
- G2 Direct: 20/20
- G3 GeneratedTests: ✓
- G4 89-fixture sweep: 89/0
- G5 axioms: `[propext, Classical.choice, Quot.sound, CertGen_faithful.{call, copy, endAbs, endBorrow_direct_witness, endBorrow_reborrow_witness, endBorrow_shared_witness, endBorrow_takeOk, join, loopInv, move, mutBorrow_direct, mutBorrow_inAbsReborrow, mutBorrow_loopOwned, reborrow, sharedBorrow, symExpandMutBorrow}]` — `sorryAx` removed
- G6 zero sorrys in `Soundness/`
- G7 warm `lake build` ~0.76s

Gates at the Phase-E1 tip (M10.4a):
- G1 vertical-slice: ✓
- G2 Direct: 20/20
- G3 GeneratedTests: ✓
- G4 89-fixture sweep: 89/0
- G5 axioms: byte-for-byte identical to M10.3a (E1 declares paper-side defs only; no new `Soundness/`-visible axioms)
- G6 zero sorrys in `Soundness/` (the two new smoke-lemma sorrys live under `LLBCSharpPaper/`)
- G7 warm `lake build` ~0.77s

### Completed commits (Session 8 — 2026-05-17, M9.8 schema-bump campaign)
- [x] M9.8a-c Cert: JrJoinMutBorrows carries fresh-abs shape; cert_fmt_version 3 → 4 — 564bce1a — 2026-05-17 (OCaml side; `JrJoinMutBorrows.abs` promoted from `abs_id` to `cert_abs_shape`; emitter populates with the canonical 3-role list `[ArMutBorrow {arg_idx=0; loan=l_l}; ArMutBorrow {arg_idx=0; loan=l_r}; ArMutLoan {loan=l_fresh}]` + `as_parent_abs=[]`)
- [x] M9.8d-f Lean: accept cert v4; JoinRule.joinMutBorrows carries AbsShape — efa613bb — 2026-05-17 (parser fmt_version 3→4; Raw `JoinRule.joinMutBorrows`'s 4th field promoted from `Nat` to `AbsShape`; paper-side `JoinEntryStep.mutBorrows` mirrored to take `AbsShape` with post-state `setAbs abs.absId (liftAbsShape abs)` — analogous to `LStep.call`'s `absSig` install; `JoinLemmas.joinMutBorrows_step` signature aligned; 18 Direct + 15 tracked llbc fixtures regen'd to v4)
- [x] M9.8g Lean: stepJoin installs Collapse-Dup-MutBorrow fresh abs in absRegistry — e0ed3c09 — 2026-05-17 (replayer's `stepJoin` folds witnesses and calls `SymState.addAbsShape` for each `joinMutBorrows _ _ _ absShape` entry, mirroring `stepCall`'s `addAbsShape` fold; cert-format-and-soundness.md §2.1 + §2.3 EvJoin doc updated)
- [x] M10.2t Soundness: stepJoin_witnessed_sound, general case — (this commit) — 2026-05-17 (signature replaces `hWitnessesEmpty + hStShape` with the more general `(Ω', hChain, hConcMatch)` triple; Phase-D will build the chain by induction over `witnesses.toList` via `JoinLemmas.join<Rule>_step` helpers and discharge `concretise st' = Ω'` from CertGen_faithful; M10.2s's empty-witnesses lemma kept as the corollary `stepJoin_witnessed_sound_empty`)

### Completed commits (Session 6 — 2026-05-17)
- [x] M10.1i Soundness: SymState.absIdHwm + stepEndAbs erases registry; move liftAbsShape paper-side — 9a6ca9b7 — 2026-05-17 (paired replayer + concretise infrastructure for the M10.0m revision; commute lemmas `concretise_addAbsShape` and `concretise_removeAbsShape` land here)
- [x] M10.0m+M10.2o-revised Soundness: LStep.call installs absSig; re-prove C15 in the full case — 89e75578 — 2026-05-17 (bundled because the LStep.call shape change forces an immediate C15 re-prove; fold-then-setLocal-then-bumpSym; new `concretise_foldl_addAbsShape` commute via Array→List.foldl induction)
- [x] M10.2p Soundness: stepEndAbs soundness (C16) — f5d4de11 — 2026-05-17 (Phase-D-dischargeable `(stPre, hConcPre, hShape)` triple folds the loan-erase + token-clear preamble; chain `concretise_removeAbsShape` ; `congrArg (·.removeAbs absId) (hConcPre.trans hRep)`)
- [x] M10.2q Soundness: stepSymExpandMutBorrow soundness (C17, no-substitution subset) — debe8ec3 — 2026-05-17 (`substLocals = #[] ∧ substLoans = #[]` Phase-D-dischargeable; one η-fold subtlety on the `{ env := st.env, ... } = st` literal record handled by `have hEtaSt ... := rfl`)

### Completed commits (Session 2 — 2026-05-17)
- [x] M10.0j Soundness: port Valid predicate (per-event premise extractor) — b099d439 — 2026-05-17
- [x] M10.0k Soundness: replace 4 axioms in StepEventSound.lean with real types — c7f8d8fb — 2026-05-17
- [x] M10.0l Soundness: Phase-A boundary review janitorial fixes — 28087ada — 2026-05-17
- [x] M10.1a Soundness: define concretise — env + numLocals — 7f1090af — 2026-05-17
- [x] M10.1b Soundness: define concretise — loans + absRegistry — 843cc93b — 2026-05-17
- [x] M10.1c Soundness: prove concretise_wellFormed_smoke — 7c2119e9 — 2026-05-17
- [x] M10.1d Soundness: concretise inversion lemmas — ac015ca8 — 2026-05-17
- [x] M10.1e Soundness: concretise commute lemmas (setLocal proved; addLoan/takeLoan sorry'd) — c670f691 — 2026-05-17

### Phase A complete (11+1 commits: M10.0a–k + janitorial M10.0l)
All paper-side surface (Syntax, State, Program, WellFormed, Step, Valid)
ported. `StepEventSound.lean` typechecks against real types; 5 sorry'd
theorems remain (4 per-event lemmas + top-level stepEvent_sound),
closed by Phases C/D.

### Phase B complete (M10.1a–e)
`concretise : SymState → LLBCState` fully defined:
- env → ctx via `liftVal` / `liftEnv`
- absRegistry → abs via `liftAbsShape` / `liftAbsRegistry`
- freshness counters from `maxKeyPlusOne` over loans + absRegistry
  (nextSymValId = 0; cert-provided monotonicity rides on CertGen_faithful)

Five inversion lemmas (`@[simp]`-tagged) read concretise's structure
field by field. setLocal commute proves; addLoan / takeLoan commute
sorry'd as Phase-C-driven discharges (hashmap-fold lemma needed).

Smoke lemma `concretise_wellFormed_smoke` discharges via
`empty_concretise + empty_WellFormed`.

### Trusted-base inventory (live)

After M10.3a (Phase D close), the trusted base is Lean core plus
the `CertGen_faithful` family (16 extractors). The four `paper_thm_*`
placeholders remain *expected* future additions but are not
introduced yet — they are not consumed by `stepEvent_sound`. Phase E
will add Phase-E members to the `CertGen_faithful` family
(`lookupFunDecl` totality, event-list well-formedness); Phase G
ports replace the `paper_thm_*` axioms with derived theorems.

| Axiom | Reason | Replaced by | Status |
|---|---|---|---|
| `CertGen_faithful.{16 extractors}` | OCaml-side honesty (broadened post-M9.7) | (forever trusted) | **live** (declared M10.3a) |
| `paper_thm_3_1_confluence` | Phase A placeholder | Phase G M10.6d | not yet declared (Phase E/F doesn't need it) |
| `paper_thm_3_3_pl_refines`  | Phase A placeholder | Phase G M10.6e | not yet declared |
| `paper_thm_4_1_safe`        | Phase A placeholder | Phase G M10.6f | not yet declared |
| `paper_thm_4_2_init`        | Phase A placeholder | Phase G M10.6g | not yet declared |

Current axiom set per `#print axioms AeneasSoundness.Soundness.stepEvent_sound` (golden:
`aeneas-lean-soundness/tests/axioms.golden.txt`):

```
propext, Classical.choice, Quot.sound,
CertGen_faithful.call, .copy, .endAbs,
CertGen_faithful.endBorrow_direct_witness,
CertGen_faithful.endBorrow_reborrow_witness,
CertGen_faithful.endBorrow_shared_witness,
CertGen_faithful.endBorrow_takeOk,
CertGen_faithful.join, .loopInv, .move,
CertGen_faithful.mutBorrow_direct,
CertGen_faithful.mutBorrow_inAbsReborrow,
CertGen_faithful.mutBorrow_loopOwned,
CertGen_faithful.reborrow, .sharedBorrow,
CertGen_faithful.symExpandMutBorrow
```

`sorryAx` is **gone**. The 16 `CertGen_faithful.*` extractors each
map 1:1 to one Phase-C per-event lemma's Phase-D-dischargeable
premises (with `endBorrow` split into 4 entries: `takeOk` for the
takeLoan-result existence and three per-LoanKind result-shape
witnesses for `.direct/.lazyExpand`, `.reborrow`, `.shared`).

### Blockers escalated to user

**RESOLVED (Session 8, 2026-05-17) — C23 general case unblocked via M9.8 schema bump**:

User picked option (A) — bump cert_fmt_version from 3 to 4 so
`JrJoinMutBorrows` carries the full `AbsShape` (id + parents +
roles) of the fresh region abstraction. The campaign shipped in
4 commits (M9.8a-c OCaml, M9.8d-f Lean parser + Raw + paper-side,
M9.8g replayer, M10.2t soundness bridge). Cert v4 is now the
mandated format; v3 and below are rejected at parse time.
Replayer's `stepJoin` installs the fresh abs in `absRegistry` via
`SymState.addAbsShape`, mirroring how `stepCall` already installs
`EvCall.abs_sig` shapes. Paper-side `JoinEntryStep.mutBorrows`
lifts the same `AbsShape` via `liftAbsShape`, so the per-entry
correspondence comes out by construction. C23's general case is
now Phase-D-dischargeable via the same `(stPre, hConcPre, hShape)`-
style triple pattern used by C11-C13 / C16; specifically, Phase D
will build `Ω'` and `hChain : JoinChain Ω witnesses.toList Ω'`
by induction over `witnesses.toList` via the per-entry
`JoinLemmas.join<Rule>_step` helpers, and discharge
`concretise st' = Ω'` from `CertGen_faithful`'s promise about
the cert's `result.env` / `result.liveLoans` shape.

The empty-witnesses subset that M10.2s closed is retained as
`stepJoin_witnessed_sound_empty` for regression anchoring; the
unrestricted form is `stepJoin_witnessed_sound`.

Gates at the M9.8 campaign tip:
- G1 vertical-slice: ✓
- G2 Direct: 20/20
- G3 GeneratedTests: ✓
- G4 89-fixture sweep: 89/0
- G5 axioms `[propext, sorryAx, Quot.sound]` (unchanged)
- G6 one expected sorry (top-level `stepEvent_sound`, Phase D)
- G7 warm `lake build` ~0.79s

**(Historical) ACTIVE (Session 7, 2026-05-17) — C23 general case = M9.8 decision needed**:

C23 (`stepJoin_witnessed_sound`) closes in Session 7 for the
`witnesses = #[]` subset. The general case, where witnesses contain
state-changing rules (`joinSymbolic` or `joinMutBorrows`), surfaces
the long-anticipated "fresh abs" gap from plan §11.1 #1 / §3.4 /
§14.1.

The semantic divergence:
* Paper's `LStep.join` chains per-entry `JoinEntryStep` rules. For
  `joinMutBorrows l_left l_right l_fresh abs`, the rule's post-state
  installs `Ω.abs abs := some { roles := {(.mutBorrow, l_left),
  (.mutBorrow, l_right), (.mutLoan, l_fresh)}, parents := #[] }`.
* Replayer's `stepJoin` (Step.lean:506-554) does NOT allocate this
  abs in `absRegistry` — it only wholesale-replaces `env` /
  `loans`. So `concretise st'` has *no* abs entry for `abs`, but
  the chain's terminal Ω' has one.

This means `concretise st' ≠ chain-terminal` for any
`joinMutBorrows` witness, breaking the load-bearing conclusion of
the lemma.

The plan anticipated three resolution paths (§14.1, §3.4, §11.1
#1); the M9.8 decision is:

**(A) — M9.8 schema bump (recommended by plan §14.1)**. Bump
   `fmt_version` to 4. Extend `JoinRule.joinMutBorrows` to carry an
   explicit `absShape : AbsShape` (or just the role list `roles :
   Array AbsRoleEntry`). Then `stepJoin` allocates the abs in
   `absRegistry` using the new payload; concretise matches; the
   per-entry helper `joinMutBorrows_step` in `JoinLemmas.lean`
   stays as-is; C23 general case closes via normal induction. Cost:
   one OCaml emitter change + cert regen for the 89 fixtures + one
   replayer extension. ~1 week of work outside the soundness
   campaign.

**(B) — Broaden `CertGen_faithful` to cover the abs creation**.
   Extend the axiom's promise so that when the cert emits
   `joinMutBorrows ... abs`, the OCaml interpreter has indeed
   created the matching `AbsShape` and the soundness side may
   *assume* it. Then add a Phase-D-dischargeable hypothesis
   `hAbsCreated : st.absRegistry[abs] = some { roles := ..., ... }`
   to the C23 lemma signature and discharge via the broadened
   axiom. **Trust drift** — the axiom now covers more than its
   §0.3 charter. Per plan §6 hard rule, this requires explicit
   user sign-off.

**(C) — Restrict C23 to no-op witness rules only (`joinSame`,
   `joinVar`, `joinBottomOther`, `joinOtherBottom`)**. Add an
   explicit `hAllNoOp : ∀ e ∈ witnesses.toList, e.rule.isNoOp =
   true` hypothesis. Phase D then has to verify this from cert
   data; many real fixtures will fail (every cert that joins on a
   mutBorrow has a `joinMutBorrows` entry). Punt to M11+.

**Recommendation**: ship Phases A–F at the empty-witnesses subset
of C23, treat option (A) as M11 / M9.8 follow-up. The end-to-end
`cert_implies_pl_safety` corollary then quantifies over crates whose
joins are degenerate (empty witnesses), which is a much weaker
guarantee but still ships the verified-pipeline architecture and
the trust skeleton. Then M9.8 happens after M10.5c as a focused
schema-bump campaign, and a follow-up M11.X closes C23 unrestricted.

A Lean attempt at option (A) is contingent on the user picking it
(the cert format is the user's call). Option (B) is a hard blocker
without user sign-off. Option (C) is doable autonomously but
weakens the corollary further.

**Next session's decision**: orchestrator should escalate to the
user *before* attempting any non-trivial subset extension of C23.
A reasonable alternative is to skip ahead to Phase D using the
empty-witnesses C23 (Phase D doesn't depend on C23's generality —
it just dispatches on `Event` constructors and passes whatever
hypotheses each per-event lemma takes; if Phase D's
`stepEvent_sound` ever encounters a non-empty join in practice, it
will fail to apply C23 and the failure is observable).

**RESOLVED (Session 6, 2026-05-17)** — the M10.0m Phase-A revision
landed cleanly. `LStep.call` now installs `absSig` in its post-state,
`stepEndAbs` erases the registry entry on the replayer side (M10.0n
folded into M10.1i), and C15 is re-proved unrestricted. C16
(stepEndAbs) and C17 (stepSymExpandMutBorrow no-substitution subset)
also closed. No active blockers; Session 7 opens cleanly with C18
(`joinSame_sound`) or the C18-C22 sibling-file refactor.

**(Historical) Phase-A inflection point (Session 5 wrap, 2026-05-17)** — surfaced
by C15 (stepCall) and confirmed by inspection of C16 (stepEndAbs):

* `LStep.call` (paper Step.lean:390-395) declares its post-state as
  `(Ω.setLocal dst.local_ (.sym σ)).bumpSymValId σ` — i.e., a single
  dst-write with no abs installation. But the replayer's `stepCall`
  folds the cert's `absSig` into `absRegistry`. The two sides
  diverge as soon as `absSig ≠ #[]`.
* `LStep.endAbs` (paper Step.lean:407-412) takes `Ω.abs abs = some r`
  as premise. Until `LStep.call` actually installs abs, this premise
  is vacuous (`Ω.abs` is identically `none`) and C16 has nothing to
  fire against.

The plan §3.4 explicitly anticipates Phase-C-driven Phase-A
strengthening: "if a per-event lemma needs one, the Phase-C commit
adds the corresponding `WellFormed` field". The strengthening
required here:

1. **Paper-side (LLBCSharpPaper)**: extend `LStep.call`'s post-state
   to fold `setAbs absSig[i].absId (paperLiftAbsShape absSig[i])`
   over the array, paired with the `regionAbs : Array AbsId` ids.
   Requires either (a) moving `liftAbsShape` / `liftAbsRoleEntry`
   into `Syntax.lean` (paper) so `Step.lean` can use them, or (b)
   adding paper-side equivalents.
2. **Replayer-side (AeneasCheck.LLBCSharp.Step)**: extend `stepEndAbs`
   to also call `st.absRegistry.erase absId` before returning, so
   `concretise st'.abs absId = none` matches `Ω.removeAbs abs`. No
   other replayer code reads erased abs entries (grep confirms only
   stepCall populates / stepEndAbs reads); G1-G4 expected green
   post-change.
3. **Valid.lean**: update `Valid.call` to existentially quantify
   per-AbsShape freshness premises (`absIdFresh` over each entry).
   Phase-D dispatch will discharge by reading `regionAbs` from the
   cert.
4. **Concretise/Defn.lean (& Lemmas)**: if step 1's helper moves to
   paper side, drop the soundness-side duplicate; add an
   `concretise_setAbs_fold` commute lemma if needed for C15.
5. **Re-prove C15** (M10.2o) with strengthened rule: chain
   `concretise_setLocal` ; `concretise_setAbs_fold` ; ...

This is roughly a 3-4 commit refactor (M10.0m for the paper-side
strengthening; M10.0n optional replayer mirror; M10.2o-revised for
the full C15). Defer ~2 sessions of Phase-C bulk if the user wants
to land C16-C17 with full absSig support; alternatively, continue
with C18-C22 (joins) in their empty-absSig restriction and revisit
the Phase-A revision later.

**Recommendation**: next session opens with M10.0m. Use the
`lean4:formalize` agent in a worktree, since the touch-set spans
3-4 files and the changes are mechanical (definition strengthening,
not deep proof work). Brief agent with the items above + the
specific `concretise.abs` shape that must match.

### Notes from Session 10 (Phase E1 open — M10.4a)

* **File-layout decision: paper / soundness split kept clean.** The
  orchestrator's commit-table mentioned a single `LLBCSharpPaper/
  InitialFinal.lean` for all three Fig. 10 predicates. But `Final`'s
  signature takes `SymState` (the replayer's running state), and
  every other file under `LLBCSharpPaper/` only depends on
  `AeneasCheck.Raw.*` — the paper-vs-checker boundary the campaign
  has held since Phase A. Co-locating `Final` with `Initial` under
  `LLBCSharpPaper/` would have forced an upward import of
  `AeneasCheck.LLBCSharp.State` (full `Std.HashMap`-typed `Val` /
  `SymState`), muddying that layering. Resolution: `Initial` +
  `BorrowChecks` + `signatureToInitialAbs` + the two smoke lemmas
  land in `LLBCSharpPaper/Program.lean` (paper-pure), and `Final` is
  a separate file `Soundness/InitialFinal.lean` that imports both
  layers and bridges them via `concretise`. Future Phase-E2/E3
  lemmas about `Final` follow the same convention.

* **`signatureToInitialAbs` shape — top-level borrows only.** The
  M10 fixture set's inputs all carry borrows at the top level
  (`&'r mut T`, `&'r T`). Nested-borrow inputs
  (`&'r mut (&'s mut T)`) are out of scope at M10 — they need a
  paired parent / child region id assignment and the M10 fixtures
  don't exercise them. The helper walks `sig.inputs` and emits one
  `AbsShape` per top-level `LlbcTy.tRef` constructor; non-borrow
  inputs (literals, ADTs without top-level borrow fields, tuples,
  …) contribute nothing. Plan §3.4 / §11.1 #5 risks earmark the
  deeper traversal as a Phase-F+ extension if a future fixture
  needs it.

* **`Initial.inputs_populated` uses paper-convention local indexing.**
  Charon's calling convention is "local 0 = return slot, locals
  `1..n` = inputs, locals beyond = body". The `inputs_populated`
  field quantifies over `1 ≤ i ≤ sig.inputs.size` rather than `i <
  sig.inputs.size` to track this. The cert format never names locals
  by index in a way that contradicts this convention; if a future
  fixture surfaces an alternative scheme, Phase E3's
  `replayFun_post` is the natural place to pin the index.

* **`BorrowChecks = True` at v4.** The cert's existence is the
  witness (the OCaml interpreter only emits certs for borrow-check-
  passing functions; `CertGen_faithful` carries this promise). The
  stronger form ("every input borrow has a matching output flow or
  is consumed by an EvEndAbs") is plan §6.1 #5 / M11. Keeping
  `BorrowChecks` trivial at v4 means `Initial_implies_borrow_checks`
  is a one-line proof when E4 needs it — but the smoke lemma stays
  `sorry`'d at E1 because we don't want Phase E2/E3 to consume the
  unfolded form prematurely.

* **`Final` fourth field is `True`-placeholder.** The cert-format §4.5
  "permitted non-direct leaks are caller-visible" condition is what
  Phase E3 (`replayFun_post`) connects to caller-visible region
  abstractions. At E1 the field exists as a hook (`True`) so Phase
  E2 can build the induction without a structural-field shift later.
  This is the same one-field-at-a-time pattern Phase C used on
  `LoanTokenInvariant` (plan §3.4).

* **`sig` parameter on `Final` documented as a Phase-E3 hook.** At
  E1 `Final`'s `sig` parameter isn't read in any field body — we
  thread it through to match cert-format §4.3's verbatim signature
  shape (so Phase E4's assembly doesn't need to renumber args). The
  field `_sig_placeholder : sig = sig := rfl` is the trivial
  satisfier that makes Lean happy with the unused parameter; Phase
  E3 replaces it with a real "permitted leaks are in
  `signatureToInitialAbs sig`-derived caller abstractions"
  condition.

* **G7 unchanged at ~0.77s warm.** Phase E1 adds ~200 LOC across
  two files (`Program.lean` extensions + new `InitialFinal.lean`)
  but no expensive elaboration paths — `Initial` / `Final` are
  small structures; `signatureToInitialAbs` is a single `Id.run do`
  with a for-loop. The build time delta is within noise.

* **Phase E2 unblocked.** With `Initial` / `Final` / `BorrowChecks`
  declared, Phase E2 (`replayFun_event_induct`) can state its
  signature as `Initial Ω_0 sig prog → ∀ idx, idx ≤ events.size →
  ∃ Ω_idx, Ω_0 ⟶_#* Ω_idx ∧ concretise (replayPrefix events idx) =
  Ω_idx` and induct over `idx`. The induction step threads
  `stepEvent_sound` per event. Two new `CertGen_faithful` extractors
  are anticipated: `event_list_wellFormed` (per-event premises
  dischargeable) and (deferred to Phase F preamble per plan §5.2)
  `lookupFunDecl_total_for_replayCrate_ok`.

### Notes from Session 9 (Phase D close — M10.3a)

* **`buildJoinChain` helper not factored out.** The orchestrator's
  M10.3a plan envisioned splitting the join arm into a
  `CertGen_faithful.join_chain` axiom (per-entry premises) + a
  `buildJoinChain` helper (induction over `witnesses.toList`
  applying `JoinLemmas.join<Rule>_step` per entry) +
  `CertGen_faithful.join_concretise_match`. In practice the
  per-entry premises depend on the *running* intermediate Ω_i (each
  step bumps freshness counters), so the natural `CertGen_faithful`
  axiom would need to expose a per-intermediate-state premise — not
  a clean trust surface. The bundled `CertGen_faithful.join` axiom
  (the full `(Ω', hChain, hConcMatch)` triple) is equally
  trust-minimal: the chain's existence + the terminal-match are
  pure facts about the trace's join algebra. Plan §0.3 charter
  (`CertGen_faithful` is the OCaml-side trace-honesty axiom) is
  preserved. `JoinLemmas.lean`'s six per-entry constructor wrappers
  remain available for callers that need a finer-grained chain
  build — Phase E may revisit if the induction proof simplifies.

* **`stepLoopInv_sound` added (C24).** Plan §3.2 row C17 originally
  named `stepLoopInv`; Session 6 substituted `stepSymExpandMutBorrow`
  and deferred the loop-fixpoint event lemma. M10.3a's
  case-analysis needed it, so it's added here with a
  Phase-D-dischargeable `hLoanRegistryEmpty : loanRegistry = #[]`
  premise (discharged by `CertGen_faithful.loopInv`). The replayer's
  `stepLoopInv` adds `.reborrow`-kind loans for unregistered ids in
  the cert's `loanRegistry`; the paper-side `LStep.loopInv` is a
  no-op. Both coincide for empty `loanRegistry`, which is the M10
  fixture set (the full case needs an M9.8-style schema bump:
  either extend `LStep.loopInv` to allow `.reborrow`-only loan
  additions, or split into a separate `loopRegisterLoan` event).

* **`Classical.choice` joined the trusted base.** Not previously
  visible in G5 because every per-event lemma was `sorry`'d; once
  they prove, the existential-witness extractions
  (`Exists.intro` / pattern-matching on `∃`-bound vars) trigger
  Classical. This is a Lean core axiom and acceptable for the
  trust surface.

* **`.proj` dispatched by contradiction.** The replayer's
  `stepEvent` returns `.error _` for `EvProj`; the paper's
  `Valid .proj _ _ _ = False`. In `stepEvent_sound`'s case-split,
  the `.proj` arm uses `simp [stepEvent] at hStep` to reduce
  `hStep : .error _ = .ok st'` to `False`, closing the arm. Cert
  v4 doesn't emit `.proj`; M11+ revival is plan §14.8.

* **`endBorrow` family split into four CertGen_faithful axioms.**
  `endBorrow_takeOk` promises the takeLoan lookup succeeds (the
  `none` case would have been a replayer failure). Then three
  per-LoanKind witnesses (`endBorrow_direct_witness` covering both
  `.direct` and `.lazyExpand`, `endBorrow_reborrow_witness`,
  `endBorrow_shared_witness`) supply the result-shape needed by
  C11/C12/C13. The dispatcher uses `cases hKind : li.kind with`
  (Lean 4 syntax) to introduce the LoanKind equation per branch.

* **Phase-D file growth.** `CertGen.lean` adds ~225 LOC of axiom
  declarations + docstrings. `StepEventSound.lean` grows ~150 LOC
  (the dispatcher case-block + `stepLoopInv_sound`). G7 warm
  ~0.76s (down from 0.79s — no significant slowdown).

* **Phase E unblocked.** With `stepEvent_sound` closed and the
  `CertGen_faithful` family installed, Phase E's `replayFun_sound`
  induction can now thread `stepEvent_sound` per iteration. Plan
  §5 lists 4 sub-commits (M10.4a-d); E1 declares
  `Initial` / `Final` / `borrow_checks#` (paper Fig. 10 ports) and
  adds `signatureToInitialAbs` to `LLBCSharpPaper/Program.lean`.

### Notes from Session 1

* **CI lane**: M10.0a scaffolds the Lake package only; adding a
  soundness CI job to `.github/workflows/ci.yml` is deferred. The
  existing ci.yml uses the nix flake which doesn't reference the
  checker by name either; the local 7-gate invocation from the
  execution prompt §0 is sufficient until a phase boundary.

* **`aeneas-lean-checker` mathlib-free**: confirmed — its lakefile
  has no `require` lines and the `RuntimeShim` library provides the
  `Aeneas.Std.*` surface that `GeneratedTests` needs without
  pulling in Mathlib. The soundness package's `require mathlib`
  doesn't leak.

* **`StepEventSound.lean` referenced nowhere**: confirmed by grep;
  no re-export shim needed in the checker. Plan §0.1 suggested one
  for safety; not required given the file was orphaned in the
  checker tree.

* **Namespace `AeneasSoundness.Soundness`**: M10.0a retargets the
  namespace from `AeneasCheck.Theorems` so the
  `AeneasSoundness.Soundness.stepEvent_sound` G5 golden line
  resolves without an alias hop.

* **`Place` not `«Place#»`**: M10.0b initially defined
  `abbrev «Place#» := Place` for paper-flavor naming, but the
  French-quote-escaped identifier (`«Place#»`) is unwieldy at every
  use site (Lean's parser doesn't accept `Place#` bare). Dropped
  the alias in M10.0f; downstream files just `open AeneasCheck.Raw
  (Place ProjElem)` and write `Place` / `ProjElem` directly.

* **Section comments inside inductive bodies**: Lean's grammar does
  not allow `/-! ... -/` between inductive constructors (the parser
  emits "expected 'lemma'" once it closes the inductive). Use
  `-- single-line` comments between constructor groups instead. Hit
  this twice in M10.0f and M10.0h.

* **Inline `-- comments` inside constructor types**: these are
  parsed as whitespace and don't break the parse, but the inductive
  may close at an unexpected place if the doc comment they precede
  isn't formally attached. Caught in M10.0f for `assign`'s
  multi-line body; resolved by removing the inline comments.

* **Doc-comment placement**: `/-- ... -/` immediately preceding a
  declaration attaches to it. If you insert other declarations
  between the doc comment and its intended target (M10.0h inserted
  `JoinEntryStep` + `JoinChain` between `LStep`'s docstring and
  `inductive LStep`), the parser errors. Convert to `/-! ... -/`
  section comments in that case.

* **`Place` field is `local_`**: `AeneasCheck.Raw.Place` calls its
  base local `local_` (trailing underscore to dodge the `local`
  keyword). Code under `LLBCSharpPaper/State.lean` and `Step.lean`
  uses `p.local_` directly.

* **`Event.matchArm` is 4-arity**: `Event.matchArm` takes
  `(scrutinee : SymExpr) (adtId variantId : Nat) (variantName :
  String)`. The plan's narrative treated it as `(adtId variantId)`
  only; the M10.0f draft had to be corrected.

* **`Multiset` notation `{x}`**: works at `Multiset
  (Role × LoanId)` for both `RegionAbs.singleton` and the
  `mutBorrows` constructor's role list; no special imports needed
  beyond `Mathlib.Data.Multiset.Basic`.

* **Per-entry join via auxiliary relation**: M10.0h introduces
  `JoinEntryStep` (6 constructors mirroring `JoinRule`) plus
  `JoinChain` (sequential composition over a list of `JoinEntry`).
  `LStep.join` then folds via `JoinChain Ω witnesses.toList Ω'`.
  Cleaner than 6 LStep variants that match `Event.join` with
  specific witness shapes.

* **Premise strength baseline**: every LStep constructor uses
  *baseline* premises that are expressible from Phase A's surface
  (place resolution, freshness counters, top-level
  `directLoanId` / `directBorrowId`). Deep-Val side conditions
  ("⊥/loan^{s,m} ∉ v"), parent-loan liveness, deref-chain
  consistency are deferred to Phase C; if a per-event lemma needs
  one, the Phase-C commit adds the corresponding `WellFormed`
  field. Per plan §3.4.

### Build & gate state at session end (tip afbe4624)

| Gate | Status | Notes |
|------|--------|-------|
| G1   | ✓     | vertical slice (M2-M8) ok |
| G2   | ✓     | 20/20 Direct fixture tests |
| G3   | ✓     | GeneratedTests 20-job build |
| G4   | ✓     | 89/0 sweep |
| G5   | ✓     | axioms.golden.txt matches |
| G6   | ✓     | no sorry under `Soundness/` |
| G7   | ✓     | warm 0.81 s, cold ~80 s (host has warm mathlib cache; budget < 30 min cold holds) |

### Notes from Session 3

* **Per-event lemma signature pattern**: The 4 sorry'd hint-bearing
  lemmas from M10.0k didn't carry `hRep` as an explicit hypothesis
  (they relied on the section's `variable (hRep : ...)`); but section
  variables don't auto-include for tactic-mode proofs that only
  reference them in tactics. Phase C lemmas added in Session 3
  carry `hRep` explicitly. The 4 still-sorry'd ones will need
  the same fix when they get proved (or we can add `include hRep`
  globally).

* **Place projection assumption (M10.2c)**: `stepMove` / `stepCopy`
  use `placeRootLocal` (the root local, ignoring projection); the
  paper-side `LStep.move` / `.copy` premise is `resolvePlace src
  = some v` which dives deep. To bridge, the soundness lemmas take
  three extra hypotheses: `src.projection = #[]`, `dst.projection
  = #[]`, and `∃ v, st.env[src.local_]? = some v`. These are
  Phase-D-dischargeable (cert emission discipline + future
  WellFormed strengthening).

* **`bumpSymValId` redefined as no-op (M10.2f)**: The paper-side
  `LStep.binop` (and several mutBorrow variants, plus call,
  symExpand) conclude with `(... ).bumpSymValId σ` which would
  bump `freshness.nextSymValId`. But `concretise` always sets
  `nextSymValId := 0` (the replayer doesn't track sym-value ids;
  `CertGen_faithful` carries cert-side monotonicity). The original
  `bumpSymValId` broke `concretise st' = Ω'`. Redefined as a no-op
  in `State.lean`. The `symValIdFresh σ` premise reduces to
  `0 ≤ σ` (trivially true). This refinement is load-bearing for
  the still-pending C8-C10, C14, C16, and C19.

* **`concretise_addLoan` is the next blocker** (M10.2g/C7 onwards):
  Every event that adds a loan (stepSharedBorrow / stepMutBorrow
  family / etc.) needs this commute lemma to discharge
  `concretise st' = Ω'`. The underlying sub-lemma is
  `maxKeyPlusOne (m.insert k v) = max (maxKeyPlusOne m) (k+1)`
  for `Std.HashMap`. Under the freshness premise
  (`maxKeyPlusOne m ≤ k`), the RHS simplifies to `k+1`, so the
  needed sub-lemma is `maxKeyPlusOne_insert_fresh`:
  ```
  maxKeyPlusOne (m.insert k v) = k + 1
    when maxKeyPlusOne m ≤ k
  ```
  Proof skeleton: `Std.HashMap.fold_eq_foldl_toList` →
  `Std.HashMap.toList_insert_perm` → `List.Perm.foldl_eq` with
  `RightCommutative max` instance → simp the result foldl over
  the unfiltered `(k,v) :: m.toList` (the freshness premise makes
  `filter (¬k == ·.1) m.toList = m.toList`). All Mathlib pieces
  exist; the proof is ~30-50 LOC. **Next session: dispatch
  `lean4:sorry-filler-deep` for this lemma in a worktree** while
  continuing other planning.

* **C7 specifically (stepSharedBorrow)**: Once `concretise_addLoan`
  closes, the proof should be straightforward — `stepSharedBorrow`
  on success returns `st.addLoan loan inner .shared` (no env
  mutation); `LStep.sharedBorrow` conclusion is
  `(Ω.bumpLoanId ℓ).bumpSymValId σ` which under the no-op
  `bumpSymValId` = `Ω.bumpLoanId ℓ` — exactly what
  `concretise_addLoan` gives us.

### Notes from Session 4

* **`if_pos` / `if_neg` over `simp only [hC, if_true/false]`**: The
  prior pattern (used in M10.2f stepBinop) of `simp only [hC,
  ite_true]` doesn't reduce `if (false = true) then ... else ...`
  because `if_false` is keyed on `if False then a else b`, not on
  the propositional `false = true`. Using `rw [if_pos hC]` /
  `rw [if_neg hC]` directly handles both the Bool-style guard
  (`st.loans.contains loan = true`) and the Prop-style guard
  (`place.local_ ≥ st.numLocals`). Cleaner and uniform across
  guards.

* **Defining `hFreshSet` for setLocal-then-addLoan**: In C8/C10
  the proof reaches `concretise ((st.setLocal _ _).addLoan loan
  _ _)`. To apply `concretise_addLoan`, the freshness premise must
  match the *intermediate* `(st.setLocal _ _).loans`. Since
  setLocal only mutates `env`, `(st.setLocal _ _).loans = st.loans`
  by `rfl`, but Lean's elaborator won't always unify with the
  underscore-filled lemma instance. Workaround: introduce
  `have hFreshSet : maxKeyPlusOne (st.setLocal _ _).loans ≤ loan
  := hLoanFresh` (the coercion is definitional), then pass
  `hFreshSet` to `concretise_addLoan`. Adds ~3 LOC per such
  lemma; worth it for clarity.

* **`concretise_takeLoan` is potentially unsound as stated**:
  The lemma claims `concretise st' = concretise st` after
  `takeLoan b = some (_, st')`. But `takeLoan` calls
  `loans.erase b`, and `maxKeyPlusOne (loans.erase b) ≤
  maxKeyPlusOne loans` (can strictly decrease if `b` was the
  max key). So `(concretise st').freshness.nextLoanId` can
  strictly *decrease*, violating the literal equality. Phase
  C's first endBorrow lemma (C11) will surface this. Likely
  fix: redefine `concretise`'s `nextLoanId` to be an *upper
  bound* counter that never shrinks (e.g., carry an explicit
  high-water mark in `SymState`, or use `max (maxKeyPlusOne
  st.loans) (cert-supplied bound)`). Defer the design choice
  to when C11 needs it — may also need a `WellFormedProgram`
  strengthening + cert-side monotonicity premise.

* **Worktree auto-merge**: The `lean4:sorry-filler-deep` agent
  with `isolation: "worktree"` was supposed to commit on a
  separate branch (`worktree-agent-<id>`); the orchestrator was
  to fast-forward merge. In practice, the harness *auto-merged*
  the worktree's clean commit into `aeneas-lean-certificate`
  directly (worktree removed; tip moved to `9be15ea7`). No
  intervention needed; verify via `git log` after the agent
  returns rather than running the merge protocol manually.

### Notes from Session 7

* **Path 1 vs Path 2 decision**: Path 1 (per-entry) chosen, with a
  pragmatic deviation from plan §3.2's 5-commit cadence. The plan
  envisioned per-entry lemmas at ~250 LOC each (bundling replayer-
  correspondence per entry). In reality, the replayer's `stepJoin`
  is wholesale (no per-entry incremental updates exist), so per-
  entry lemmas can only be paper-side constructor wrappers — one-
  line `JoinEntryStep.<rule> <freshness-or-existence-premise>`
  facts. Five such facts collapsed naturally into one M10.2r commit
  (~50 LOC total). The "5-wide parallel batch" §3.3 suggested was
  not worth the orchestration overhead for trivial lemmas.

* **`JoinChain.nil` qualification**: `JoinChain` is not in the
  `open AeneasSoundness.LLBCSharpPaper (LLBCState LStep Valid)`
  list at the top of StepEventSound.lean, so the literal
  `JoinChain.nil` is unknown. Fully-qualified `LLBCSharpPaper.
  JoinChain.nil` works. (`LStep.join` resolves because `LStep` is
  in the opened list.) If future join-cluster lemmas need richer
  chain manipulation, consider broadening the `open` line.

* **C23 empty-witnesses subset trivially closes**: Once
  `witnesses = #[]`, the `Valid.join` existential reduces to
  `⟨Ω, JoinChain.nil⟩`, `LStep.join` reduces to
  `LStep.join JoinChain.nil`, and `concretise st' = Ω` collapses
  to `hStShape.trans hRep` for the Phase-D-dischargeable hypothesis
  `hStShape : concretise st' = concretise st`. 5-line proof body.
  Total lemma + docstring: 47 LOC of insertions.

* **"Fresh abs" gap is real, not theoretical**: confirmed by
  inspection of `stepJoin` (Step.lean:506-554). The replayer never
  touches `absRegistry`. Any non-`joinSame`/`joinVar`/`joinBottom*`
  witness would force the chain's terminal Ω' to include an abs
  entry that `concretise st'` lacks. The M9.8 schema bump (option
  A in the blocker write-up) is the cleanest fix; option (B) drifts
  trust; option (C) restricts the corollary. See Blockers section.

### Notes from Session 6

* **`absIdHwm` mirrors `loanIdHwm`** — same pattern as M10.1g but
  for abs ids. Bumped by `stepCall`'s fold (via the new
  `SymState.addAbsShape` helper) and untouched by `stepEndAbs`'s
  `removeAbsShape` (which only `absRegistry.erase`'s, not the
  HWM). `concretise.freshness.nextAbsId` retargets from
  `maxKeyPlusOne st.absRegistry` to `st.absIdHwm`, making the
  M10.1i `concretise_addAbsShape` and `concretise_removeAbsShape`
  commutes both unconditional. Two new bridging hashmap lemmas
  `liftAbsRegistry_insert` / `liftAbsRegistry_erase` mirror
  `liftEnv_insert`.

* **`liftAbsShape` moved paper-side** — into
  `LLBCSharpPaper/Syntax.lean`, alongside `Role` /  `RegionAbs`.
  The `AeneasCheck.Raw.CertEvent` import was added to `Syntax.lean`
  to bring `AbsShape` into scope (no cycle: `CertEvent` only
  depends on `Places` / `Literal`). Soundness-side `Concretise/Defn.lean`
  re-`open`s the moved names so existing call sites work. The one
  `Concretise.liftAbsShape`-qualified reference in
  `StepEventSound.lean` (C8b mutBorrow_inAbsReborrow) updated to
  `LLBCSharpPaper.liftAbsShape`.

* **`LStep.call` post-state order matches `stepCall`** —
  fold-then-setLocal-then-bumpSym. The fold step is
  `installAbsShapePaper Ω' shape := (Ω'.setAbs shape.absId
  (liftAbsShape shape)).bumpAbsId shape.absId`. No freshness
  premise needed on `absSig` — the `max`-based bump commutes with
  the lift even if `shape.absId` is non-fresh (overwrites are
  fine; `bumpAbsId` is idempotent on already-bumped HWMs).
  `Valid.call` stays as just `∃ σ, Ω.symValIdFresh σ` (no extra
  premise).

* **`concretise_foldl_addAbsShape` lemma** — fold-commute that
  pushes `concretise` through `absSig.foldl SymState.addAbsShape`.
  Proved by `rw [Array.foldl_toList, Array.foldl_toList]` then
  induction on `absSig.toList`. The cons case is one `ih` + one
  `concretise_addAbsShape` hop + `rfl`. ~10 LOC.

* **C16 stepEndAbs uses the result-shape pattern from C11-C13**
  — but with one extra layer: the replayer's `stepEndAbs` has two
  preamble for-loops (loan-erase + token-clear) followed by
  `removeAbsShape absId`. The `(stPre, hConcPre, hShape)` triple
  folds the preambles into a single `concretise`-preserving step.
  The proof itself is one `congrArg (·.removeAbs absId)
  (hConcPre.trans hRep)` once `concretise_removeAbsShape` has
  pushed `concretise` past the removeAbsShape.

* **C17 stepSymExpandMutBorrow needs the η-fold trick** — after
  empty-array `for` loops, `hStep` carries the literal record
  `{ env := st.env, loans := st.loans, ... }` which is
  *definitionally* equal to `st` but `simp` won't reduce it.
  Workaround: one `have hEtaSt ... := rfl` line proves the record
  equality, then `rw [hEtaSt] at hStep` cleans it up. Worth
  remembering for any future lemma that hits empty-for-loop
  reduction. This same shape may also surface in stepEndAbs if
  we ever try to drop the result-shape hypothesis.

* **C18-C22 (joins) deferred to Session 7** — the replayer's
  `stepJoin` (lines 506-554 of `AeneasCheck/LLBCSharp/Step.lean`)
  does a *wholesale* replacement of `st.env` from `result.env`
  and prunes `st.loans` against `result.liveLoans`. The paper-side
  `LStep.join` chains per-entry `JoinEntryStep` rules via
  `JoinChain`. These two semantics don't align trivially — the
  replayer doesn't compute per-entry transitions, it just
  installs the post-state directly. Plan §3.3 recommends
  factoring C18-C22 into sibling files under
  `AeneasSoundness/Soundness/JoinLemmas/Join<Rule>.lean` so they
  can parallelise. C20 (`JoinMutBorrows`) is the known-hard one
  (plan §6.1 #5 — likely M9.8 trigger; the paper's
  `Collapse-Dup-MutBorrow` introduces a fresh abs the cert may
  not name).

### Notes from Session 5

* **loanIdHwm redesign**: the Session 4 caveat ("`concretise_takeLoan`
  is potentially unsound as stated") forced a SymState shape change.
  `SymState` gains a monotone `loanIdHwm : Nat := 0` field bumped by
  `addLoan` and never touched by `takeLoan` / `loans.erase`. The
  replayer's checker logic never reads this field — it exists solely
  for soundness. `concretise.freshness.nextLoanId` retargets at
  `st.loanIdHwm` (was `maxKeyPlusOne st.loans`). Two consequences:
  `concretise_takeLoan` closes (no field changes either side);
  `concretise_addLoan` becomes *unconditional* (the LHS `loanIdHwm
  := max old (b+1)` matches the RHS `bumpLoanId b := max old (b+1)`
  by `rfl`). The freshness premise on C7-C10 lemmas renames from
  `maxKeyPlusOne st.loans ≤ loan` to `st.loanIdHwm ≤ loan`; uses of
  `concretise_addLoan _ _ _ _ hFresh` drop the explicit witness.
  Sym-arg `absIdHwm` not yet needed (no event in the current LStep
  surface erases from absRegistry; if endAbs lemma ever does, same
  fix applies).

* **End-borrow trio (C11-C13) contract**: rather than unroll the
  replayer's `for ... in st.env.toList` env-scan loop, each lemma
  takes a Phase-D-dischargeable *result-shape* hypothesis pinning
  st' directly:
  - C11 (.direct / .lazyExpand): `(x, v, stTake, hShape : stepEvent
    ... = .ok (stTake.setLocal x v))` + `hHolder : Ω.ctx x =
    some (.mutLoan loan)`.
  - C12 (.reborrow): `(stTake, hShape : stepEvent ... = .ok stTake)`.
  - C13 (.shared): same as C12.
  Phase D discharges `hShape` and `hHolder` from the `LoanTokenInvariant`
  uniqueness fact (plan §3.4 strengthening). This is the same
  "Phase-D-dischargeable" pattern used by C2-C10 for place-projection
  and env-existence hypotheses.

* **`LocalId` not in scope** at `Soundness/StepEventSound.lean`:
  `LocalId := Nat` lives in `LLBCSharpPaper`, but the file's
  `open AeneasSoundness.LLBCSharpPaper (LLBCState LStep Valid)` is
  selective — `LocalId` is not opened. Lean auto-binds the
  identifier as a polymorphic type variable (`Sort u_1`), which
  triggers a confusing "of sort Sort u_1 but is expected to have type
  ... of sort Type" error. Workaround: use `Nat` directly in
  parameter signatures (matches the pattern of `place.local_ : Nat`
  in the mutBorrow lemmas). If future lemmas need many `LocalId`
  / `AbsId` parameters, broaden the `open` line.

### Session boundaries

- **Session 1: 2026-05-17, commits M10.0a → M10.0i** (9 commits).
  Started from the post-M9.7 baseline; the four axioms in
  `StepEventSound.lean` are still in place but the paper-side
  surface they will be replaced by is now ported (Syntax, State,
  Program helpers, WellFormed, all 24+4 LStep constructors).

- **Session 2: 2026-05-17, commits M10.0j → M10.1e** (8 commits).
- **Session 3: 2026-05-17, commits M10.2a → M10.2f** (6 commits).
  Phases A *and* B closed. `Valid` predicate ported; `StepEventSound.lean`
  migrated to real types; janitorial fixes from the boundary review
  applied (M10.0l). `concretise` fully defined with env/absRegistry/
  freshness; inversion + setLocal commute lemmas in place. Two
  deferred sorrys: `concretise_addLoan` / `concretise_takeLoan`
  commute lemmas (hashmap-fold equality is the missing step;
  Phase C closes when first lemma needs them — likely M10.2h
  `stepMutBorrow_direct_sound`). G6 still exempt
  (Phase C starts at commit #17; we're at #16).

- **Session 4: 2026-05-17, commits M10.1f → M10.2j** (3 commits).
  C7–C10 closed. `concretise_addLoan` unblocked via a deep agent
  that landed `maxKeyPlusOne_insert_fresh` (~140 LOC helper block
  in `Concretise/Defn.lean`). The agent's worktree auto-merged
  cleanly. C8–C10 batched in one commit; they share the if-guard
  reduction (`rw [if_pos hC] at h; cases h` for the loan-conflict
  branch and the bounds-check branch), the `loanIdFresh`
  derivation from `hLoanFresh : maxKeyPlusOne st.loans ≤ loan`,
  and the `concretise_setLocal ; concretise_addLoan` chain.
  10/23 Phase-C done. Next: C11–C13 (endBorrow trio) which
  needs `concretise_takeLoan` — currently sorry'd and possibly
  unsound as stated (`erase` can shrink `maxKeyPlusOne`; see
  Session 4 notes).

- **Session 5: 2026-05-17, commits M10.1g → M10.2o** (5 commits).
  loanIdHwm redesign (M10.1g checker addition + M10.1h soundness
  retarget) resolved the Session 4 caveat; `concretise_takeLoan`
  closes cleanly, `concretise_addLoan` becomes unconditional. C11-C13
  (endBorrow trio) batched into M10.2k-m via Phase-D-dischargeable
  result-shape hypotheses, avoiding the env-scan loop unroll.
  C14 (stepReborrow) closes with `hParentInHwm` no-op trick on
  pre-add-parent branch. C15 partial (empty-absSig only); the
  non-empty case surfaced a Phase-A surface gap in LStep.call that
  also blocks C16 — see Blockers section for the M10.0m plan.
  15/23 Phase-C done (one partial). Tip 922c0bb0.

  Gate state at session end (922c0bb0): G1 ✓ vertical slice;
  G2 ✓ 20/20 Direct fixtures; G3 ✓ GeneratedTests; G4 ✓ 89/0 sweep;
  G5 ✓ axioms.golden.txt matches (`propext, sorryAx, Quot.sound`);
  G6 ✓ only expected sorrys (`stepJoin_witnessed_sound` C18-C22 +
  top-level `stepEvent_sound` D); G7 ✓ warm 0.8s.

- **Session 7: 2026-05-17, commits M10.2r → M10.2s** (2 commits).
  Phase C now at 23/23 lemmas declared. M10.2r bundles C18-C22 as
  six paper-side `JoinEntryStep` constructor wrappers in a new
  `Soundness/JoinLemmas.lean` (deviates from plan's 5-commit
  cadence; see Session 7 notes). M10.2s closes C23
  (`stepJoin_witnessed_sound`) in the `witnesses = #[]` subset;
  the general case is the campaign's deepest open issue, escalated
  to user as an M9.8 schema-bump candidate (see Blockers section).
  Tip 49ef3650.

  Gate state at session end (49ef3650): G1 ✓ vertical slice;
  G2 ✓ 20/20 Direct fixtures; G3 ✓ GeneratedTests; G4 ✓ 89/0 sweep;
  G5 ✓ axioms.golden.txt matches (`propext, sorryAx, Quot.sound`);
  G6 ✓ only one expected sorry remaining (top-level
  `stepEvent_sound`, the C23 sorry is gone in the empty-witnesses
  subset); G7 ✓ warm 0.77s.

- **Session 6: 2026-05-17, commits M10.1i → M10.2q** (4 commits).
  Phase-A inflection point closed. M10.1i adds `SymState.absIdHwm`
  (monotone mirror of `loanIdHwm`), retargets
  `concretise.freshness.nextAbsId` at it, extends `stepEndAbs` to
  erase the absRegistry entry, and moves `liftAbsShape` /
  `liftAbsRoleEntry` from the soundness-side `Concretise/Defn.lean`
  into the paper-side `LLBCSharpPaper/Syntax.lean`. M10.0m strengthens
  `LStep.call`'s post-state to fold `setAbs ; bumpAbsId` over `absSig`
  (fold-then-setLocal-then-bumpSym order, matching the replayer); C15
  is re-proved unrestricted (M10.2o-revised) in the same commit. M10.2p
  (C16 stepEndAbs) and M10.2q (C17 stepSymExpandMutBorrow,
  no-substitution subset) follow the same Phase-D-dischargeable
  result-shape pattern as C11-C13. 17/23 Phase-C done; only C18-C22
  joins remain. Tip debe8ec3.

  Gate state at session end (debe8ec3): G1 ✓ vertical slice;
  G2 ✓ 20/20 Direct fixtures; G3 ✓ GeneratedTests; G4 ✓ 89/0 sweep;
  G5 ✓ axioms.golden.txt matches (`propext, sorryAx, Quot.sound`);
  G6 ✓ only expected sorrys (`stepJoin_witnessed_sound` C18-C22 +
  top-level `stepEvent_sound` D); G7 ✓ warm ~1.5s.

- **Session 10: 2026-05-17, commit M10.4a** (1 commit). Phase E1
  opens. Paper Fig. 10 type-only foundation lands:
  `signatureToInitialAbs`, `BorrowChecks`, `Initial` in
  `LLBCSharpPaper/Program.lean` (paper-pure); `Final` in a new file
  `Soundness/InitialFinal.lean` (bridges to `SymState` via
  `concretise`). Two smoke lemmas `sorry`'d under LLBCSharpPaper/
  (G6-exempt). No new G5 axioms — byte-for-byte golden match with
  M10.3a. Phase E2 unblocked: `replayFun_event_induct` is the next
  commit's target.

  Gate state at session end: G1 ✓ vertical slice; G2 ✓ 20/20 Direct
  fixtures; G3 ✓ GeneratedTests; G4 ✓ 89/0 sweep; G5 ✓ golden
  byte-for-byte match with M10.3a; G6 ✓ zero sorrys in `Soundness/`;
  G7 ✓ warm ~0.77s.

### Notes from Session 2

* **G6 exemption clarified**: Plan §10.1 line 697 spells out that
  G6 runs "After every Phase-C/D/E/F/G commit (i.e., commits #17
  onward)". Per-event `sorry`s in `StepEventSound.lean` (M10.0k)
  + addLoan/takeLoan commute sorrys (M10.1e) are *not* G6
  violations — Phase C closes them.

* **`.proj` events**: Per `Valid.lean`, `Valid (.proj _ _ _) :=
  False`. Vacuously consistent with the iff smoke lemma. Phase D's
  case-split needs `False.elim` (or refute via the replayer's
  rejection of proj events at `Replay.lean:50`). No Phase-A test
  fixture emits `.proj` (confirmed by review).

* **Phase-A boundary review GREEN**: doctor + review agents
  returned 7 nits + 5 design observations; 3 nits applied as
  M10.0l. The two "soft block" caveats — `LStep.assign`
  unconstrained `v` and `endBorrow` constructor dispatch — were
  documented as deliberate design intents in `Step.lean` rather
  than fixed in the constructors; they reflect the existential-v
  / replayer-LoanKind-driven dispatch contract for Phase D.

* **Open Phase-C contracts** (capture before C1 lands):
  - The "Phase D delegation contract" for `endBorrow` /
    `mutBorrow` constructor dispatch is recorded in
    `Step.lean`'s top-level docstring (M10.0l). When Phase C
    lemmas commit to a signature, the replayer-side LoanKind /
    MutBorrowKind drives the LStep constructor choice.
  - `concretise_addLoan` / `concretise_takeLoan` commute lemmas
    are sorry'd in `Concretise/Lemmas.lean` (M10.1e). The
    hashmap-fold equality
    `maxKeyPlusOne (m.insert k v) = max (maxKeyPlusOne m) (k+1)`
    is the load-bearing missing step; close it before M10.2h
    (`stepMutBorrow_direct_sound`).

* **JoinDeterminisation axiom NOT introduced yet**: per plan
  §11.1 #1, this was an anticipated Phase-A axiom. The Phase-A
  ports got through without needing it (the cert's `JoinEntry`
  array is the deterministic ordering). May land if a Phase-C
  join lemma needs it.
